name: Run local cold reviews

on:
  workflow_dispatch:
    inputs:
      expected_commit:
        description: Exact main commit to review
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: programme-gate-local-cold-reviews
  cancel-in-progress: false

jobs:
  review:
    if: github.ref == 'refs/heads/main'
    runs-on: [self-hosted, macOS, ARM64, canonical-review]
    environment: programme-gate-production
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.expected_commit }}
          fetch-depth: 0
          persist-credentials: false
      - name: Verify exact clean main commit
        env:
          EXPECTED_COMMIT: ${{ inputs.expected_commit }}
        run: |
          set -euo pipefail
          test "${GITHUB_SHA}" = "${EXPECTED_COMMIT}"
          test "$(git rev-parse HEAD)" = "${EXPECTED_COMMIT}"
          test -z "$(git status --porcelain)"
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install locked dependencies without protected credentials
        run: npm ci
      - name: Run protected local cold reviews
        env:
          EXPECTED_PROGRAMME_COMMIT: ${{ inputs.expected_commit }}
          LOCAL_CODEX_AUTH_FILE: /Users/bengoodchild/.codex/auth.json
          PROGRAMME_GATE_REVIEW_CONTROLLER_ED25519_PRIVATE_KEY_PEM: ${{ secrets.PROGRAMME_GATE_REVIEW_CONTROLLER_ED25519_PRIVATE_KEY_PEM }}
          PROGRAMME_REVIEW_OUTPUT: ${{ runner.temp }}/programme-gate-cold-reviews.json
        run: |
          set -euo pipefail
          umask 077
          node scripts/run-g0-local-cold-reviews.mjs
          test -s "${PROGRAMME_REVIEW_OUTPUT}"
      - name: Upload signed cold reviews
        uses: actions/upload-artifact@v4
        with:
          name: programme-gate-cold-reviews-${{ inputs.expected_commit }}
          path: ${{ runner.temp }}/programme-gate-cold-reviews.json
          if-no-files-found: error
          retention-days: 7
      - name: Remove runner output
        if: always()
        run: rm -f "${RUNNER_TEMP}/programme-gate-cold-reviews.json"
