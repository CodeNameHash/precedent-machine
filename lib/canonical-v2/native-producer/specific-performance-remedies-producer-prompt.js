'use strict';
const PROMPT_ID = 'native-producer-specific-performance-remedies/v1';
const PROMPT_VERSION = 2;
const RESPONSE_SHAPE = `{"remedy_assertions":[{"assertion_kind":"SPECIFIC_PERFORMANCE | DAMAGES_WAIVER | LITIGATION_EXTENSION | EXPEDITED_PROCEEDING","quote":"<one verbatim remedy mechanism>"}],"open_world_candidates":[]}`;
const INSTRUCTIONS = `Read specific-performance and remedies provisions only. Produce exact positive evidence. For a SPECIFIC_PERFORMANCE assertion that states the operative injunction, specific-performance, or equitable-relief grant, its one contiguous quote must also retain the full immediately preceding premise that irreparable harm would occur and money damages would not be an adequate remedy. Do not emit a bare equitable-relief grant. Preserve express condition gates, termination limits, non-waiver language, bond or security waivers, and other limits in the same exact quote when they govern that remedy. Capture specific performance, including an express injunction, equitable-relief or force-consummation right, only when the quote states it. Capture a damages waiver, litigation extension, or expedited proceeding only when the quote states it. A sole-remedy or fee-election clause belongs to termination fees. Do not infer availability of a remedy, calculate a period, or classify fee triggers. Put any other remedy text in open_world_candidates. Never assert a negative. Return JSON only.`;
function buildSpecificPerformanceRemediesProducerPrompt({ source_text: sourceText, governed_scope: governedScope }) { if (typeof sourceText !== 'string' || !sourceText) throw new TypeError('source_text must be a non-empty string'); if (!governedScope || typeof governedScope !== 'object') throw new TypeError('governed_scope must be an object'); return { prompt_id: PROMPT_ID, prompt_version: PROMPT_VERSION, messages: [{ role: 'user', content: [INSTRUCTIONS, 'RESPONSE SHAPE:', RESPONSE_SHAPE, 'SOURCE TEXT:', sourceText].join('\n\n') }] }; }
module.exports = { PROMPT_ID, PROMPT_VERSION, RESPONSE_SHAPE, INSTRUCTIONS, buildSpecificPerformanceRemediesProducerPrompt };
