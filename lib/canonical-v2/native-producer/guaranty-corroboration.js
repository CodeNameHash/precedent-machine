'use strict';
const GUARANTY_ASSERTION_KINDS = Object.freeze(['PERFORMANCE_GUARANTY', 'GUARANTY_DELIVERED', 'GUARANTY_IN_EFFECT']);
const tests = Object.freeze({ PERFORMANCE_GUARANTY: (quote) => /\bguarantees\b/i.test(quote) && /\bperformance\b/i.test(quote) && /\bobligations?\b/i.test(quote), GUARANTY_DELIVERED: (quote) => /\b(delivered|furnished)\b/i.test(quote) && /guarant(y|ee)/i.test(quote) && (/\bduly executed\b/i.test(quote) || /true,? (complete and correct|correct and complete) copy/i.test(quote)), GUARANTY_IN_EFFECT: (quote) => /full force and effect/i.test(quote) && /guarant(y|ee)/i.test(quote) && /valid and binding/i.test(quote) });
function corroborateGuarantyKind({ quote, asserted_kind: kind } = {}) { if (typeof quote !== 'string' || !quote) throw new TypeError('quote must be a non-empty string'); if (!GUARANTY_ASSERTION_KINDS.includes(kind)) return Object.freeze({ outcome: 'OPEN_WORLD', reason: 'GTY_ASSERTION_KIND_OUT_OF_VOCABULARY' }); if (!tests[kind](quote)) return Object.freeze({ outcome: 'REVIEW', reason: 'GTY_KIND_UNCORROBORATED' }); const performance = tests.PERFORMANCE_GUARANTY(quote); const delivery = tests.GUARANTY_DELIVERED(quote) || tests.GUARANTY_IN_EFFECT(quote); if (performance && delivery) return Object.freeze({ outcome: 'REVIEW', reason: 'AMBIGUOUS_GUARANTY_OBJECT' }); return Object.freeze({ outcome: 'RESOLVED', assertion_kind: kind }); }
module.exports = Object.freeze({ GUARANTY_ASSERTION_KINDS, corroborateGuarantyKind });
