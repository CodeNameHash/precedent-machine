'use strict';
const PROMPT_ID = 'native-producer-key-terms-mae-follow-on/v2'; const PROMPT_VERSION = 2;
const RESPONSE_SHAPE = `{"mechanics":[{"surface":"DEF_PRODUCT_OUTPUT | ACQUISITION_THRESHOLD | SUPERIOR_THRESHOLD | ORDINARY_COURSE_STRUCTURE | EFFECTIVE_THRESHOLD_RELATIONSHIP","quote":"<verbatim mechanism>","detail":"<factual description>"}],"open_world_candidates":[]}`;
const INSTRUCTIONS = `Capture exact evidence for deferred key-defined-term mechanics. Labels are routing only. Do not calculate an effective threshold or assign a no-shop or condition result. Keep acquisition and superior proposal definitions with the key-terms owner. Keep closing conditions and no-shop rights in their own families. The MAE definition producer owns MAE disproportionality, its comparison baseline, its source form, and its relationship to each carve-out limb. Do not duplicate those MAE facts here. Never assert a negative. Return JSON only.`;
function buildKeyTermsMaeFollowOnProducerPrompt({ source_text: sourceText, governed_scope: governedScope }) { if (typeof sourceText !== 'string' || !sourceText) throw new TypeError('source_text must be a non-empty string'); if (!governedScope || typeof governedScope !== 'object') throw new TypeError('governed_scope must be an object'); return { prompt_id: PROMPT_ID, prompt_version: PROMPT_VERSION, messages: [{ role: 'user', content: [INSTRUCTIONS, 'RESPONSE SHAPE:', RESPONSE_SHAPE, 'SOURCE TEXT:', sourceText].join('\n\n') }] }; }
module.exports = { PROMPT_ID, PROMPT_VERSION, RESPONSE_SHAPE, INSTRUCTIONS, buildKeyTermsMaeFollowOnProducerPrompt };
