# Asset sweep: lib/parser-v2, lib/schema, lib/vocab, lib/provisions

Status: COMPLETE.

## Byte-offset conversion helper (deliverable #7)

**Path:** `lib/canonical-v2/canonical-bytes.js` (outside this slice — lib/canonical-v2/, but reported per instructions since it's a named, specific deliverable).
**Exports:** `utf8ByteLength(value)` and `utf8Slice(value, start, end)`.

`utf8ByteLength` returns `Buffer.byteLength(String(value), 'utf8')`. `utf8Slice(value, start, end)` takes half-open BYTE offsets, slices the UTF-8-encoded buffer, decodes back to a string, and throws `RangeError` if the offsets don't land on code-point boundaries (so a slice that would split a multi-byte character fails loudly instead of returning corrupted text) or if start/end aren't valid integers. This is the module actually used throughout `lib/canonical-v2/` to convert between JS string (UTF-16 code unit) coordinates and the byte offsets canonical-V2 stores/serves (confirmed via real requires in `definition-graph.js`, `metsera-comprehensive-selection-review.js`, `metsera-pilot-extension-proposal.js`, `parser-proposal-adapter.js` — not comment mentions). Notably `lib/canonical-v2/parser-proposal-adapter.js:57` converts a V1 parser CHAR offset into a byte offset with `utf8ByteLength(text.slice(0, charOffset))` — i.e. this is precisely the seam where V1's char-offset world (subclauses.js, span-claims.js, resolve-source-span.js — all plain JS string indices, confirmed above) meets V2's byte-offset world. Any code that pipes a V1 offset into a V2 byte-offset field, or vice versa, must go through this conversion or an equivalent — none of the three span-accounting files in this slice do it themselves, because they're internally self-consistent in char-offset space and never claim to produce bytes.

## Summary

Most of lib/parser-v2's "structural layer" is not missing from canonical-V2 — it is already reused, via a hash-pinned manifest (`canonical-structural-definitions.manifest.json`) that runs structural.js, regions.js, all five regions/*.js detectors, coverage.js, text-layers.js, enacting-hard-wall.js and defined-terms-toc.js inside a sandboxed runtime (`lib/canonical-v2/admitted-parser-proposal-adapter.js`) for the definitions family, and separately reuses classify.js + structural.js directly (not sandboxed) for no-shop via `lib/parser-v2/canonical-proposals.js` -> `lib/canonical-v2/reviewed-no-shop-slice.js`. The genuinely-unadopted, high-value finds are the span-accounting trio (span-residual.js, span-claims.js, resolve-source-span.js — all char-offset, all deterministic, all tested, zero live canonical-v2 callers beyond one comment), the three boundary invariants not in the manifest bundle (list-continuity.js, provision-completeness.js, sentence-integrity.js), attribute-provision-section.js's content-based drift-resilient attribution, and three deterministic single-concept extractors (elections.js, notice-advisors.js, advisors.js) that V2 has no equivalent of. lib/schema/ (minus features.js/tags.js as raw content and the separately-owned topology-detector.js) is entirely V1 review-UI plumbing with zero canonical-v2 callers, and its README is stale in exactly the way CLAUDE.md's opening warns about (claims features.js is "empty until Phase 3"; it holds 551 populated definitions). The byte-offset conversion helper is `utf8ByteLength`/`utf8Slice` in `lib/canonical-v2/canonical-bytes.js` — outside this slice but the exact seam where V1's char-offset world meets V2's byte-offset world.

## Detail: span accounting trio (span-residual.js, span-claims.js, resolve-source-span.js)

**span-residual.js** (`computeSectionResidual(sectionText, claimedSpans) -> { sectionChars, leafCount, residualChars, residualRatio, flagged, flagReason, drops }`). Takes a section's raw text, runs `segmentSubClauses` (subclauses.js) to get Part-1 leaves, and subtracts everything covered by `claimedSpans` (the union of every emitted item's claimed spans for that section, from span-claims.js). Ignores leaves < 200 chars and "benign chapeau" leaves (marker:null, matching a small set of transitional-phrase regexes, e.g. "the following", "subject to the foregoing"). Flags `EXTRACTION_INCOMPLETE` if residualRatio > 0.25 OR any single unclaimed leaf > 1500 chars. Header explicitly says this ONLY computes the flag — no enforcement, that's future work (rollout gates 2/3, not built). Code matches header exactly.

**span-claims.js** (`computeSpanClaims(sectionText, items) -> items with {claimedSpans, spanUnlocated, textSpan} attached`, non-mutating). For each item (any object with a `.text`/`.quote`/`.full_text` field), locates its text in the section via a 3-pass locator (exact indexOf -> whitespace-tolerant regex -> same regex on first 160 chars only, mirroring lib/verification.js's locateProvisionInSource pattern) and reports which Part-1 leaves it overlaps. `spanUnlocated: true` when text can't be found anywhere = hallucination surface. Also exports `attachSpanClaimsToProvisions(provisions, sectionTextByStartChar, opts)` — explicitly INERT unless called with `{spanClaims: true}`; when live it writes into `prov.features.spanClaims`, not top-level provision fields. Header calls out THREE separate span coordinate spaces in the repo (section-relative here; provision_cards.primary_quote_start/end relative to region_full_text; resolve-source-span.js's own marker-stripped-fulltext-relative space) and warns against conflating them — this is itself a useful map for anyone touching spans.

**resolve-source-span.js** — WP-5 module resolving a stored provision-card quote into offsets against the marker-STRIPPED deal full_text that SourceOverlay renders (full_text is laden with `[[REF]]`/`[[DEFINED]]` tags). `stripWithOffsetMap(rawText)` builds a bidirectional raw<->stripped index map. `resolveSourceSpan({fullText, quoteStart, quoteEnd, primaryQuote, regionFullText})` tries, in order: (1) explicit stored offsets IF `fullText.slice(quoteStart,quoteEnd) === primaryQuote` exactly, (2) exact/whitespace-normalized find of primaryQuote in stripped text, (3) same against the wider regionFullText, (4) `status: 'unresolved'` — never guesses. Header states store-cards.js's `primary_quote_start/end` are NOT absolute full_text offsets despite an earlier plan's claim — they're relative to the card's own `region_full_text` excerpt (verified against live data per the header) — a documented discrepancy worth carrying forward. `resolveCardSourceSpan(card, fullText)` is the convenience wrapper actually imported by `pages/review/[id].js` and `components/review-v2/SourceOverlay.jsx`.

**Coordinate space, ALL THREE**: every offset here is a plain JS string index (UTF-16 code unit), not a byte offset. None of the three files call any byte<->char conversion helper, and none needs to internally (they're self-consistent — computing and consuming their own offsets against the same string representation). The hazard is only at the boundary: if a caller elsewhere in canonical-V2 stores byte offsets (per CLAUDE.md's "byte offsets, not string indices" warning) and feeds them into `resolveSourceSpan`'s `quoteStart/quoteEnd` params expecting char-index semantics, offsets will silently be wrong on any non-ASCII deal text (curly quotes, em-dashes, section symbols are common in SEC filings). See byte-offset helper section below.

Tests: `tests/span-residual.test.js`, `tests/span-claims.test.js`, `tests/span-claims-wiring.test.js`, `tests/resolve-source-span.test.js` — run via `CI=true node --test <files>` (this repo uses node's built-in test runner, NOT jest — `npm test` confirms). Result: 40/40 pass, 0 fail.

Callers (real, not comment-only): `attribute-provision-section.js` imports `locateInSection` from span-claims.js live; `validate.js` imports `computeSectionResidual` live (Part 3 report path); `extract.js` imports `attachSpanClaimsToProvisions` live but gated inert-by-default; `scripts/span-residual-baseline.js` (the corpus-wide report-only runner, `reports/span-residual-baseline.json` exists — has been run) imports both `computeSpanClaims` and `computeSectionResidual`; `pages/review/[id].js` and `components/review-v2/SourceOverlay.jsx` import from resolve-source-span.js live, for the V1 review UI's "View in agreement" / SourceOverlay highlight feature.

**canonical-V2 usage**: `grep -rn` against `lib/canonical-v2/` and `scripts/canonical-v2*.mjs` finds exactly one hit, in `lib/canonical-v2/phase1-authority-boundary-inventory.js:122-124`, and it is a COMMENT ("Depends on ./span-claims so it cannot be PRODUCTION_PATH_PURE_ANALYSIS ... its own test and by scripts/span-residual-baseline.js"), not a require. **Canonical-V2 does not actually call any of these three modules.** This is exactly the pattern CLAUDE.md warns about — a mention that isn't a dependency — and it cuts the other way here: it means the residual-proof and span-claim machinery genuinely IS available to adopt into canonical-V2's extraction path, not already wired in under a name I'd miss.

## Inventory table

| path | status | why it matters now |
|---|---|---|
| lib/parser-v2/span-residual.js | ASSET | Deterministic residual-coverage proof (Part 3 of span-accounting spec) — computes unclaimed leaf spans per section; direct fit for #1 (residual proof) |
| lib/parser-v2/span-claims.js | ASSET | Deterministic locator that maps emitted item text -> section sub-clause leaves + textSpan; hallucination surface via `spanUnlocated`; direct fit for #1 |
| lib/parser-v2/resolve-source-span.js | ASSET | Marker-strip + offset-map + 4-tier fallback resolver mapping a stored quote into SourceOverlay's marker-stripped coordinates; used live by pages/review/[id].js and SourceOverlay.jsx (V1 review UI, not canonical-V2 extraction) |
| lib/parser-v2/attribute-provision-section.js | ASSET | Content-based (not position-based) provision->section attribution that survives classified_sections snapshot drift, with explicit tie-break + legacy-fallback chain; direct pattern for absence-vs-zero discipline (#6) — 470/952 baseline flags were attribution misses, not real drops |
| lib/parser-v2/canonical-proposals.js | ASSET (already live) | Bridges V1 classify.js `tryDeterministic` + structural.js `parseStructure` into a canonical-V2-shaped, hash-verified proposal batch for no-shop; genuinely required (not commented) by lib/canonical-v2/reviewed-no-shop-slice.js — proof V2 already reuses V1 classification for one family, template for doing it elsewhere |
| lib/parser-v2/canonical-structural-definitions.js + .manifest.json | ASSET (already live, MAJOR) | Hash-pinned manifest lets canonical-v2's admitted-parser-proposal-adapter.js run structural.js + regions/* + text-layers.js + coverage.js + invariants/enacting-hard-wall.js + detectors/defined-terms-toc.js + edgar-cleanup.js inside a sandboxed runtime for the definitions family — most of the "structural layer" is NOT missing from V2, it is already wired in this way |
| lib/parser-v2/structural.js | ASSET (already live via manifest above) | 124KB deterministic regex sectionizer/article-context builder; grepped only, not read in full; live in V2 for definitions detection via the sandbox, not via direct require |
| lib/parser-v2/regions.js, regions/atomic.js, regions/backmatter.js, regions/definitions.js, regions/double-dummy.js, regions/preamble.js | ASSET (already live via manifest) | Region-type taxonomy + per-region detectors (atomic single-concept sections, backmatter exhibit/annex/schedule typing, definition-unit boundary detection with bracket/quote-depth-aware end scanning, double-dummy equity detection, preamble TOC/recitals/enacting split) — all in the sandboxed bundle |
| lib/parser-v2/coverage.js | ASSET (already live via manifest) | Region-level gap/overlap invariant (`coverageDiagnostics`/`assertCoverage`) — coarser granularity than span-residual.js's sub-clause-leaf residual; complementary, not duplicate |
| lib/parser-v2/text-layers.js | ASSET (already live via manifest) | `buildLayers` — raw-to-clean text normalization (zero-width chars, smart quotes, stray-pipe repair, boundary-noise), with an offset map back to raw; live in V2 for definitions |
| lib/parser-v2/invariants/enacting-hard-wall.js | ASSET (already live via manifest) | Prevents a section's body-start from landing before the "NOW, THEREFORE" enacting clause |
| lib/parser-v2/detectors/defined-terms-toc.js | ASSET (already live via manifest) | Strips "Index of Defined Terms" TOC blocks (term/section-ref pair runs) before structural parsing |
| lib/parser-v2/invariants/list-continuity.js | ASSET (NOT yet in V2) | Detects a monotonic lettered/numbered list run that would continue across a proposed section boundary — used only by extract.js + its own test, absent from the manifest bundle and from canonical-v2 entirely |
| lib/parser-v2/invariants/provision-completeness.js | ASSET (NOT yet in V2) | Flags an "invalid terminator" (a region ending mid-list-item or on a stub sentence) needing boundary extension — same non-adoption status as list-continuity.js |
| lib/parser-v2/invariants/sentence-integrity.js | ASSET (NOT yet in V2) | `isLegalSentenceBoundary`/`extendToSentenceBoundary` — decides whether a region boundary lands on a real sentence end vs mid-sentence, extends it if not; same non-adoption status |
| lib/parser-v2/detectors/consideration-grouping.js | PARTIAL (V1-only) | Classifies whether multiple equity-instrument treatments share one sentence/paragraph vs get their own subprovision; used only by consideration-equity.js, no dedicated test found, not in canonical-v2 |
| lib/parser-v2/detectors/transaction-steps.js | PARTIAL (V1-only) | Extracts ordered merger steps (tender/first-step/second-step) from STRUCT/CONSID/DEF sections; depends on schema/topology-detector.js (owned by another agent — not analysed here); used by extract.js + scripts/backfill-transaction-steps.js only |
| lib/parser-v2/elections.js | PARTIAL (V1-only, candidate detector win) | Deterministic election-mechanism detector with an explicit negative-guard regex pair (distinguishes "no election available" clauses from real elections) — exactly the kind of concept-detector ask #5 wants; zero canonical-v2 references |
| lib/parser-v2/notice-advisors.js | PARTIAL (V1-only, candidate detector win) | Deterministic outside-counsel-per-side extractor from NOTICES sections (party-block split + firm/attention-line capture); zero canonical-v2 references |
| lib/parser-v2/advisors.js | PARTIAL (V1-only, candidate detector win) | Deterministic financial/legal advisor extractor scanning preamble+signature block against a large canonical-firm alias table; zero canonical-v2 references |
| lib/parser-v2/parse-json.js | CONSIDERED AND REJECTED by V2 | Prose-tolerant JSON extractor (fence/prose-wrap/truncation-repair, first-object-wins). One canonical-v2 script (scripts/canonical-v2-f28-third-live-extraction-run.mjs) genuinely requires it, but the production LLM path (lib/canonical-v2/native-producer/anthropic-provider.js) has its OWN narrower, stricter parser and a comment explaining why: parse-json.js's truncation-repair and first-match-wins behavior are unsafe for V2 (an ambiguous or truncated response should fail loudly, not get guessed at). The same comment flags that this file's own header used to incorrectly claim it delegated to parse-json.js — a stale-header instance worth knowing about if anyone edits anthropic-provider.js's header next |
| lib/parser-v2/consideration-equity.js, extraction-checkpoint.js, format-renderer.js, reapply-corrections.js, region-store.js, run-extract.js, snapshot.js, store.js, store-cards.js, store-claims.js, extract.js | IRRELEVANT (V1 orchestration/persistence) | V1 pipeline-internal orchestration, checkpointing, DB writing and UI-token rendering; no canonical-v2 references found; not reusable as atomic assets for V2's extraction quality question |
| lib/schema/card-model.js, coverage.js, empty-policy.js, formatters.js, index.js, prompt.js, provision-card.js, summary.js, types.js, validation.js | IRRELEVANT (V1 review-UI/rubric plumbing) | All tightly coupled to V1's own FEATURES/TAGS registries and used only by V1 review pages/scripts; zero canonical-v2 requires found for any of them |
| lib/schema/features.js, features.generated.js, tags.js, tags.generated.js | MENTIONED, NOT USED | Large content-only registries (551 feature defs / tag defs); grep-only per instructions. canonical-v2 hits are exclusively comments citing a line number for cross-reference context (e.g. `termination-product-projection.js:247`, `legacy-query-mapper.js:124`) — never a require. Exactly the "mention vs dependency" trap CLAUDE.md warns about, confirmed here the boring way (it really is just comments) |
| lib/schema/README.md | STALE HEADER | States "features.js: canonical feature registry, empty until Phase 3" and "Phase 2 intentionally does not wire the registry into extraction" — false today: features.js has 551 populated definitions and validation.js/index.js actively use it in V1. A second confirmed instance of the exact failure mode CLAUDE.md opens with |
| lib/vocab/ | CONFIRMED, unchanged | 3 real files (ioc-categories.js, trigger-code-aliases.js, party-role-aliases.js), 1 partial (ioc-components.js), 3 empty Phase-A scaffolds (ioc-other-exclusions.js, rw-general-lookback-scopes.js, rw-sec-filings-portions-excluded.js) — left untouched per instructions |
| lib/provisions/apply-patch.js | IRRELEVANT | V1 review-UI correction-write path (PATCH provisions + log correction row); nothing to do with extraction quality |
