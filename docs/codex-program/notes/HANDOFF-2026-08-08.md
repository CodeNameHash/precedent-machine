# Handoff — 8 August 2026

Read this first if you are picking up this work. The branch
`claude/codex-handoff-plan-status-77wn7n` is **merged to `main`** (`c40b7bb1`).
It says where the work is, where every artefact lives, what is blocked and why,
and what to do next. It exists because this project's most expensive habit is
forgetting what it has already built.

---

## Where we are, in one paragraph

Four staged resolver fixes landed and are measured. A seven-slice sweep of the
whole repository found working V1 machinery canonical-V2 was not using, and one
piece of it — `segmentSubClauses` — is now adopted. REPRESENTATIONS was
extracted across four new deals. The corpus was measured properly for the first
time and the headline finding is that **resolution rate tracks structural depth
inversely across all 25 families**. Step 2X is written into `PLAN.md`, reviewed
by Fable, and corrected where Fable found the plan's load-bearing claim was
false. **Step 2X is now partly implemented** — see the status table below. The
**MERGED to `main` as `c40b7bb1` on 2026-08-08** — 207 commits, after all
three blockers were closed and the gates ran green on a quiet machine. The
sections below are kept as the record of how that was reached.

---

## The merge approach — decided 2026-08-08

**Originally: merge the record now, hold the code. In the end both merged
together**, because the three blockers closed faster than expected and holding a
207-commit branch open had its own cost. The split-merge reasoning is kept below
because the blocker discipline it produced is what made the merge safe.

**Final status, 2026-08-08. All three blockers CLOSED; merged.** Blocker 1
closed by integrating 2X-D with a replay proving nothing was lost. Blocker 2
closed by two independent reviewers working separate slices — one real defect
found and fixed, no test weakened to make a change pass. Blocker 3 closed as far
as this environment allows.

Gates on a quiet machine, exit codes captured to files and read back rather than
piped: `CI=true npm test` exit 0 — 8,319 tests, 8,274 pass, **0 fail**, 45
skipped; `npm run build` exit 0; `forbidden-patterns.sh` exit 0, INVARIANT-4
PASS. Re-verified **after** the merge, because `main` carried commits from PR
#480 that neither pre-merge run had seen: post-merge suite exit 0, build exit 0.

Step 2X sub-steps now landed on this branch:

| step | state | evidence |
|---|---|---|
| 2X-D revert the MAE split | **DONE** | replay: resolved count unchanged at 32, all 13 claims reclassify to `MAT_MAE_QUALIFIED`, zero residuals, zero quarantines |
| 2X-L REPRESENTATIONS limb shaper | **LANDED** | first non-empty `limb_component_trees` in 202 runs — 2 trees, 7 path nodes, 6 assertion nodes |
| 2X-L1 account for every limb | **DONE** | 69-limb disposition table published; replay regenerated without `MAT_MAE_AGGREGATE` — see `step-2x-l1-limb-disposition.md` |
| 2X-E absence copy | **DONE** | 14 unsafe strings across 11 configs now use `CONDITION_ABSENT_COPY`; zero remain |
| 2X-A structure service + placement | **DONE (replay)** | `resolveGoverningStructure` + placement annotation; live termination adapter swap deferred |
| 2X-A1 qualifier scope | **DONE** | `NAMED_SUBSET`; `in any case` retired as false friend |
| 2X-B corroboration fallback | **HOLD (report-only)** | scaffolds gated; corpus pass 3 GC / 0 GTY / 0 TAX would-resolves — awaiting Ben |
| 2X-C collision report | **DONE (enforced)** | §16 LITNOTIFY precedence; MERGESUB tightened; held collisions 0 |
| 2X-H input tokens | **DONE** | cache fields summed into `input_tokens` |
| Derived limb schema | **DESIGNED** | `DERIVED_LIMB/V1`; stability survey 0 start moves; mint still gated |
| Termination structural swap | **DONE** | 19/19 byte-identical Modiv/Concho/TopBuild |
| 2X-J vocabulary dispositions | **DONE** | unaccounted 0; see step-2x-j-vocabulary-dispositions.md |
| 2X-G open-world promotion | **DONE (gate + first promo)** | deal-count gate; REQUEST_RETURN → NOSOL-CEASE; skywater 11→10 |
| 2X-F topology detector | **DONE (detector half)** | 7/7; UNDETERMINED / PARALLEL_MERGERS / REVERSE_TRIANGULAR_THEN_LLC |
| 2X-I prompt bump | **IN FLIGHT** | IOC V1×25 + MAE limbs + 2F2 schema landed (`aae4823f`); mutual two-row mint + model `transaction_steps`/precedence next; then digest close |
| 2X-K | **NEXT after 2X-I** | Modiv live ladder authorised at this rung |

The original three blockers, with their outcome:

1. **CLOSED 2026-08-08.** ~~The MAE materiality split is still in the code and
   we have already decided it is wrong.~~ Integrated from
   `cursor/step-2x-d-mae-revert-945e`. The retired code is gone from the live
   path; the duplicate key in `taxonomy.js` is documented rather than deleted,
   because deleting either entry would break stored V1 claims. One premise in
   its own comment was stale on arrival and has been corrected — see below.
   Original text: Ben ruled to revert it (Step 2X-D, `DECISIONS.md` entry 14).
   Merging now ships a taxonomy decision that makes two deals with identical
   legal effect stop matching in precedent search. Red Hat carries 13 claims on
   the code being retired — I first reported 26, having counted raw string
   occurrences rather than claims.
2. **CLOSED.** Eleven commits — not seven — were tagged UNREVIEWED or wip.
   Reviewed by two agents split by area. One real defect: two termination-limb
   unit tests carried a fixture missing `canonical_text_id`, fixed by computing
   the real digest rather than a placeholder. Both reviewers independently
   confirmed no test was weakened, and the Redfin §2.10 expectation — the
   cheapest one to soften — is untouched. Original text: they were committed to
   satisfy the stop hook while agents were mid-flight, not because the diffs
   were reviewed. `CLAUDE.md` forbids committing unreviewed delegate output; the
   tags are honest labelling, not compliance. The absence-copy port across 11
   config files is the one to look hardest at — its author's account has been
   read, its diff has not.
3. **Closed as far as this environment allows.** The app builds and serves;
   `/` and `/login` return 200. A review page could not be rendered — every
   route redirects to `/login` and no credentials are available here — so the
   changed surface was verified at the component-input level instead: zero
   unsafe absence strings remain across the 11 configs, and the string they
   render resolves to "Not found (may not be present, or not yet extracted)".
   Stronger than a green build, weaker than a screenshot. Original text: The absence change alters
   what a reviewer sees across 11 families. A green build is not a working page.

Order to unblock, as actually worked: revert the MAE split (DONE) →
live-verify what can be verified here (DONE) → diff-review the eleven WIP
commits against their criteria (two reviewers, by area) → re-run the three gates
on a settled tree → merge.

**Scale warning for whoever reviews it.** 74 source files outside tests and
fixtures, ~12,240 inserted lines; counting fixtures and evidence the diff is far
larger. `candidate-resolution.js` and
`scripts/canonical-v2-live-extraction-run.mjs` carry the most change. A real
review, not a skim — which is why it was split by area across two reviewers.

---

## Where everything is

### The plan and the record

| what | where |
|---|---|
| Step 2X, sub-steps 2X-0 and A–L (incl. A1 and L1) | `docs/core/PLAN.md`, between Step 2H and Stage 3 |
| What closed today, with evidence | `docs/core/COMPLETED.md`, "Step 2X-PRE" at the end |
| Ben's seven rulings, with reasoning | `docs/core/DECISIONS.md`, entry 14 |
| Every sweep finding, with its disposition | `docs/codex-program/notes/sweep-disposition.md` |
| Fable's review and the 2X-A decision | `docs/codex-program/notes/fable-review-step-2x.md` |
| Whether derived limbs need identity | `docs/codex-program/notes/derived-structure-identity.md` |
| Depth and `(x)/(y)/(z)`, with the four measurements | `docs/codex-program/notes/subclauses-depth-and-xyz.md` |
| Merge review, canonical-v2 slice | `docs/codex-program/notes/merge-review-canonical-v2.md` |
| Merge review, scripts/UI/tests slice | `docs/codex-program/notes/merge-review-scripts-ui-tests.md` |

### The sweep — seven slices, one file each

All under `docs/codex-program/notes/`:

- `asset-sweep-lib-root.md` — `lib/` top level
- `asset-sweep-parser-schema.md` — `parser-v2`, `schema`, `vocab`, `provisions`
- `asset-sweep-lib-subdirs.md` — the remaining `lib/` subdirectories
- `asset-sweep-ui.md` — `pages/` and `components/`, including expected rows per family
- `canonical-v2-capability-inventory.md` — what V2 already does, in four states
- `asset-sweep-scripts-sql.md` — the scripts keep-list, the schema map, the dead
- `asset-sweep-tests.md` — 33 reusable rules, the replayable fixtures
- `topology-investigation.md` — the 4/7 result and the detector's failure modes
- `cross-family-consistency.md` — absence copy and what else should generalise
- `revalidation-ladder.md` — the Modiv-first sequencing and why

### The code that matters

| what | where |
|---|---|
| The limb segmenter, now with the colon rule | `lib/parser-v2/subclauses.js` |
| Where V2 consumes it | `lib/canonical-v2/native-producer/mae-clause-label-parse.js` |
| The shaper registry — 2X-L fixed the REPRESENTATIONS entry here | `lib/canonical-v2/native-producer/anthropic-provider.js:3353` |
| The whole-document section tree, already live | `lib/canonical-v2/native-producer/deterministic-sectionizer.js` |
| Termination grammars, IOC corroboration, resolution | `lib/canonical-v2/native-producer/candidate-resolution.js` |
| Materiality codes, and the duplicate key defect | `lib/taxonomy.js` lines 105 and 143 |
| Whole-clause-then-flag, the pattern to copy | `lib/bring-down-tiers.js` |
| Absence copy constant | `lib/canonical-conditions.js`, `CONDITION_ABSENT_COPY` |
| Byte/character conversion | `lib/canonical-v2/canonical-bytes.js` |
| Expected rows per family, with PW question numbers | `lib/category-summary-features.js` |

### Evidence and artefacts

Extraction evidence is `evidence/canonical-v2/<deal>-<family>-<date>-<tag>/`.
Modiv has 62 directories, TopBuild 33 — the two deepest baselines, which is why
the re-validation ladder starts there.

Two published artifacts for Ben:
- Decisions and corpus output (earlier in the session)
- State, recommendations and decisions —
  `https://claude.ai/code/artifact/7d3e770d-af83-4643-a184-2bf3c8a9435b`,
  source at `docs/core/`-adjacent scratchpad `state-note.html`

---

## The finding everything hangs on

Resolution rate by family, `resolved / review_queue`, all 2026-08-08 runs:
**52.8% overall (1,415 of 2,679), with 3,031 open-world candidates.**

Flat, one-fact-per-sentence families resolve at 100% — misc-boilerplate,
tax-matters, material-contracts, merger-structure. Families drafted as a chapeau
over lettered limbs resolve at 13–26% — financing covenants 13%, termination
17%, representations 22%, interim operating 26%.

The resolver is not failing. It is handed fragments with no parent to inherit
from and correctly refuses to guess. That is the whole argument for Step 2X.

---

## Traps this session created or hit. Do not repeat these.

**A run receipt is not the model's output.** The receipt records what the
*shaper produced*. Concluding that a producer emits nothing because the receipt
shows nothing is wrong, and it was the load-bearing error in the first draft of
Step 2X. Red Hat's REPRESENTATIONS responses contain 69 limbs; the receipt shows
none, because the shaper discards them. **Read `raw_response_text`.**

**`review_queue` is the attempted set, not a reject pile.** It carries every
attempted claim with `has_resolution` and `auto_pass` flags. Adding it to
`resolved` double-counts and produced a reported 34.6% corpus rate that should
have been 52.8%. The rate is `resolved / review_queue`.

**`pgrep -fc` matches the checking shell's own argv.** It reported four builds
running when there was one, and earlier reported work alive when everything was
dead. Count via `/proc` with the checking shell excluded.

**Never pipe test output into `head` or `tail`.** A pipeline returns the last
command's exit code and will report success on a failing suite. Redirect to a
file, `echo $?`, then grep the file.

**Byte offsets are UTF-8; `indexOf` and `slice` are UTF-16.** `subclauses.js`
works in string indices and must stay that way — it has 8 passing tests and four
live V1 consumers. Convert at the boundary.

**Background agents die silently.** Two did today and neither notified. Verify
by artefact — did the file grow, did the branch move — not by notification.

---

## What to do next, in order

1. **Merge the docs to `main`.** No blockers.
2. **Step 2X-L** — make the REPRESENTATIONS shaper read the limbs it is already
   given. Highest value in the step, costs zero model calls, unblocks the
   largest family in the corpus. Path hygiene is an acceptance criterion:
   model limb paths mix outline markers with descriptive headings.
3. **Step 2X-D** — revert the MAE split and fix the duplicate key. This also
   clears merge blocker 1.
4. Diff-review the seven WIP commits, live-verify, merge the code.
5. Then the rest of Step 2X in the order 2X-K sets: the free replay-validated
   phase, one prompt bump, then the live phase.

---

## Open with Ben

- **Component nesting.** `provision_components` are flat under an instance —
  there is no component-to-component parent. Ben's narrow → limb → clause needs
  two levels; the schema supports one. This is a small, concrete schema
  question, not a design one.
- **`ACTIONS` vs `EFFECTS`** tagging, inconsistent across deals — with Fable.
- Everything else in `DECISIONS.md` entry 14 is settled and should not be
  re-litigated as preference.
