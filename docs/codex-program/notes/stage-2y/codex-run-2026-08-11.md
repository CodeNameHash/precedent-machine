# Codex — the corrected run brief

**Supersedes `codex-overnight-2026-08-10.md`, which was red on arrival.** An
adversarial review ran the suite rather than reading it and found the brief
walked the agent into a deadlock, plus an arithmetic error in its own success
criterion. Both are fixed here.

Read `docs/core/ROUTE-TO-GOOD-EXTRACTION.md` for why the plan changed. The short
version: the diagnosed register tops out around 51–77% placed, not 85%; the one
cohort ever measured delivered 22% of plan; and ~280 of 2Y-A's 575 fragments
land back in open-world unless 2Y-H exists first.

---

## The brief

```
Branch `claude/codex-handoff-plan-status-77wn7n`. Fetch fresh — it moves.
Read `CLAUDE.md`, `docs/core/OPERATING-RULES.md`, `docs/core/DECISIONS.md`
entry 17, and `docs/core/ROUTE-TO-GOOD-EXTRACTION.md`.

THE ONE REPORTING RULE, AND IT OVERRIDES EVERY COUNT BELOW
  Recovery is the RESOLVED-COLUMN DELTA, per family, snapshotted after every
  numbered item. Nothing else counts as recovery. Clearing review flags is
  reported in the review column. Removing open-world rows is reported in the
  open-world column. Do not sum across columns and call it recovery — the
  previous brief did exactly that and its own stop condition would have fired
  on the arithmetic rather than on a real finding.

0. FIX THE RED TREE FIRST. Nothing below is measurable on a red tree.
   `CI=true npm test` currently fails in three independent places on b7d84c1d.
   a) Seven tests in `tests/canonical-v2-human-anchor-review.test.js` demand
      `evidence/blind-review/2026-08-10/stage-2y-0-human-anchor-machine-packet.json`,
      which is not committed. RESOLUTION: commit the deterministically
      generated machine packet. It needs no human — the SITTING is what waits,
      not the packet. Bump the schema version and update the `exactKeys`
      validator in the same change or it will not load.
   b) `tests/canonical-v2-20260808-blind-current-rescore.test.js` pins the
      CATEGORY stratum at 1; your fixes produce 5. The fix is right, the pin is
      stale. Update the pin and say so in the commit message.
   c) `tests/canonical-v2-f28-live-run-pins.test.js` — your resolver changes
      broke a BYTE-FOR-BYTE receipt pin on a committed live run. DO NOT
      regenerate this pin. Produce a diff showing exactly what changed in the
      resolution output and STOP on this item. A human reviews it. Regenerating
      it unattended would mask whatever moved.

1. RE-RUN THE 46-CALL REGRESSION BATCH, before any resolver change.
   Every later measurement needs a clean baseline. Measuring resolver work on
   top of an unfixed producer regression attributes nothing.
   WATCH FOR: `LEGACY_RESTRICTION_CATEGORY_QUOTE_ALIASES` matches `source_quote`
   BYTE-IDENTICALLY. Fresh model output that rephrases a quote silently
   un-fixes SETTLE and CHARTER. Check this explicitly and report it.

2. SPLIT THE GATE. RESOLVE (into the review queue, live, no calibration) vs
   PUBLISH (unattended, gated). Test proving a loosened check resolves a claim
   while REQUIRE_PUBLISHED stays false. Nothing in this run publishes.

3. ACTIVATE, EACH REPORTED IN ITS OWN COLUMN
   2Y-I: 104 measured → RESOLVED column. This is the only real recovery here.
   2Y-G: 59 + 107 → OPEN-WORLD column. Resolved delta zero. Say so.
   2Y-F: 57 roots → REVIEW column. These clear flags on already-resolved
         claims. Resolved delta zero. Say so.
   2Y-B: per its CORRECTED spec only — KDT-definition-first, model fallback
         only where the agreement defines nothing. Blind deferral was ruled
         wrong once already. If the 18 missing KDT records block it (SkyWater
         13, TopBuild 5), say so and move on; do not guess a definition.
   Expected honest resolved delta for this whole item: ~104, up to ~195 if
   2Y-B lands. NOT 220. NOT 311.

4. FINISH 2Y-N AND SHIP THE RENDERED-ROWS ARTEFACT
   Extend beyond Closing Conditions and Termination to every anchor family.
   Emit a committed browsable artefact under `evidence/`: 30-50 REAL claims as
   excerpt -> governing chapeau -> the row a lawyer would see (section, row
   label, every cell). Include at least one case where the CODE is right and
   the ROW is wrong — one absence-copy row, one mis-banded `bandAligned` row.
   That contrast is the entire point of the step.
   Check `codex/m3-employee-dno-build` (3978f282) for Closing Conditions
   projection work before writing new projection code.

5. THE 2Y-A BINDING CENSUS — REPORT-ONLY. THIS IS THE MOST VALUABLE ITEM HERE.
   The existing census records `candidate_host_binding_performed: false`.
   Availability is not yield. Extend it to ATTEMPT binding, report-only:
   per family, how many fragments bind, and of those how many CLASSIFY.
   Commit the artefact. Touch no disposition. Change no resolution.
   This converts the largest unknown in the whole plan into a number. If you
   do nothing else tonight, do this.
   NOTE THE COUPLING: 209 of the 575 are REPRESENTATIONS and 71 are
   KEY_DEFINED_TERMS. A bound representations fragment becomes a limb-assertion
   candidate and lands back in open-world unless 2Y-H exists. Report bind and
   classify separately so this is visible.

6. DO NOT ACTIVATE 2Y-A THIS RUN.
   Stage 2 activation requires: the `context_rung` / `context_ladder_mode` seam
   in `resolveCandidates`; UTF-16 boundary conversion via `canonical-bytes.js`;
   no corroborator handed concatenated whole-section text; and a per-deal
   resolution-set diff proving ZERO previously-resolved claims changed value or
   state. If item 5's bind-and-classify count comes in under half what stage 2
   would need to be worth activating, STOP AND REPORT THE CENSUS.

7. MEASURE. Two committed artefacts under `evidence/`.
   (a) Four states per family — attempted, resolved, open-world, review —
       snapshotted AFTER EVERY NUMBERED ITEM, not once at the end. Attribution
       must survive an interrupted run.
   (b) The blind sample re-scored PER STRATUM against the twelve strata of
       eight in `evidence/blind-review/2026-08-08/README.md`. Never a bare
       total — the current "24 beats 21" depends on fallback quote matching,
       and under strict matching PARTY_UNRESOLVED falls from 5/8 to 1/8.

NOT THIS RUN
  2Y-A activation (item 6). 2Y-H topic classification — Claude is writing the
  topic list. Anything that serves or publishes.

STOP CONDITIONS — stop, push what is green, report
  - The f28 receipt-pin diff (item 0c). A human reviews that one.
  - Open-world RISES in any family.
  - Any previously-resolved claim changes value or state.
  - A gate cannot be made to pass honestly. Never weaken a test to make a
    change pass.

GATES BEFORE EVERY PUSH
  `CI=true npm test`, `npm run build`, `bash scripts/lint/forbidden-patterns.sh`.
  Capture exit codes to files and read them back. Never pipe test output into
  head or tail. Run tests and build SEQUENTIALLY — a concurrent run produces a
  spurious build failure.

TRAPS
  - Byte offsets are UTF-8; indexOf and slice are UTF-16. Convert at the
    boundary with `lib/canonical-v2/canonical-bytes.js`.
  - Read the code, not the comment.
  - A run receipt is not the model's output — read `raw_response_text`.
  - `review_queue` is the attempted set, not a reject pile.
  - A family returning zero can be correct.
  - Search before concluding something does not exist.

REPORT
  Per item: the four-state delta per family, and which column moved. Anything
  you chose not to do, stated explicitly. Silent scope-narrowing is the failure
  mode this programme watches for hardest.
```
