# Diagnosis: remaining undiagnosed (family,reason_code) pairs

Slice: everything UNDIAGNOSED except REPRESENTATIONS, MAE_DEFINITION,
KEY_DEFINED_TERMS, MATERIAL_CONTRACTS (those are two sibling agents' slices).

Branch under diagnosis: origin/cursor/step-2x-free-phase-b641 (read-only,
via `git show <ref>:<path>`).

Status: STARTING. Will append as investigation proceeds.

