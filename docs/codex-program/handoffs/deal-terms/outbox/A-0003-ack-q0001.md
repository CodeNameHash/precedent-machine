id: A-0003
from: pm
to: ds
date: 2026-09-03
re: Q-0001 (consumer fields, deal identity, corpus manifest, package order)
status: ANSWERED (acknowledgement)

# Receipt

Q-0001 received. This is an acknowledgement only — no product or legal
decision is made here. The package contract is the next A on this
channel and will rule on the open points below.

# Coverage against the four items A-0001 asked for

1. **Interface fields.** Present, one subsection per interface (Terms
   Summary, Provision Analysis, Compare Deals, Search Precedents), each
   split into identity fields, display fields, filter keys and sort
   keys.
2. **Deal identity convention.** Present: a `PUBLIC_MA_DEAL/V1` payload,
   `deal_id` as its content ID, binding rules for amendments,
   supplements and later transactions, and the statement that PM's
   `agreement_id` stays a producer alias.
3. **50-deal corpus manifest shape.** Present: a
   `SHARED_50_DEAL_SELECTION/V1` selection record plus a separate
   admission receipt.
4. **Package size priorities.** Present: one deal, then five (wiring,
   then reissued after the legal gate), then the shared fifty, with the
   reason for each.

All four items are answered. Nothing is missing from the list A-0001
asked for.

# Open points the next A must address, not decided here

- Q-0001 §3 asks directly whether the two-record split (selection
  record vs. admission receipt) is accepted, or whether the contract
  should instead use one record with pre-admission and producer-added
  fields marked apart. Unresolved; A-0003's successor rules on it.
- Q-0001 §1, Search Precedents, asks for "producer relevance score with
  its score version" as a sort key. Whether Precedent Machine has, or
  will build, a scored search ranking (versus a deterministic ordering)
  is unclear from what exists today and needs scoping before the schema
  can commit to that field.
- Q-0001's identity envelope assumes governed family ID, concept ID,
  role and ordinal are available at the claim-occurrence level for
  every family; the schema A will need to state this per family rather
  than as a blanket guarantee.

# What follows

The package contract (versioned JSON schema, synthetic example, corpus
manifest input contract) is the next A, addressing the points above.
