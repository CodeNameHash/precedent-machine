id: A-0022
from: lead
to: ext
date: 2026-09-04
re: Q-0024 review of the Work 2 real-text successor (PR #489 @ 5c818926)
status: CHANGES

# Verdict: CHANGES

Verified live, not from the note: `CI=true node --test` exits 0 on your test file (3/3) and on `tests/stage-2y-structure-m7-v2-repair-contract.test.js` (250/250); the ten-agreement run reproduces Q-0024's counts exactly (`MATERIAL_SPAN_UNMODELLED` 55, `NO_SINGLE_PROFILE` 4202, `PARTY_PROOF_UNPROVED` 2101, ten analyses, zero failures); a second run is byte-identical (21 of 21 sha256 matches across analyses, attempt records and the report); nothing was written under `control/` or `receipts/`. But `validate` exits 2 with FAIL on all ten and the per-agreement detail is `M7_V2_GOVERNANCE: analysis.governance has an open or incomplete shape`, not the approval-state check the Acceptance text describes, so the guard's substantive checks have never run and the delivery note's `FAILED_EXPECTED` claim is asserted rather than derived. The PR's one CI failure is `CANDIDATE_BINDING_DRIFT` on the sealed Work 4 test, caused by D1's authorised change to the generator bytes against the hash pinned in registration `9a3ccbf7…`; that is mine to clear, not a defect in this diff. What blocks acceptance is that the generator's own output cannot pass `validateAnalysisV2` once the governance shape is fixed, and that five spec items are unbuilt.

# Required changes

1. **BLOCKER** `lib/canonical-v2/m7-v2-deterministic-generator.js:2470-2493`, `lib/canonical-v2/m7-v2-contract.js:6043` (D1-generator:F1, D2-contract:02). Only `source_closures` is deduped by authored unit; `candidate_sets`, `authored_unit_effect_ledgers`, `coverage_partitions` and `dispositions` stay `compiled.map(...)`, one per claim. The contract admits one candidate set (:6043) and one effect ledger (:11342) per authored unit and one coverage partition per closure (:10903, `indexBy` at :718), and the rule `exactKeys` list (:11980-11985) has no `linked_rule_ids`. On the repo's own `twoOccurrenceGeneratorFixture` this throws `M7_V2_CANDIDATE_SET: an authored unit has more than one candidate set`; 285 M2 nodes carry more than one claim across the ten agreements. Spec D1(a), D2(e). Emit one candidate set, one effect ledger and one coverage partition per authored unit, each carrying one inspected effect or ledger entry per claim, and admit `linked_rule_ids` on the rule shape with a symmetry check as part of the D2 change. Re-plan §4.1's "the validator already tolerates it" is false.
2. **BLOCKER** `lib/canonical-v2/m7-v2-deterministic-generator.js:589`, `lib/canonical-v2/m7-v2-contract.js:6056-6067` (D1-generator:F2, D2-contract:03). `compileReviewOnlyOccurrence` adds `candidate_profile_ids` to every inspected effect; the candidate-effect `exactKeys` list does not admit it, so a review-only occurrence throws `candidate effect has an open or incomplete shape`. Phase 1's expected outcome is every occurrence review-only, so no analysis can pass the guard. This is masked today by item 5's governance throw, which fires first: the current ten FAILs are not evidence of it, and the requirement stands regardless. Spec D1(b), D2. Either record the ids where the contract already admits them (`effect.profile_results`, `disposition.all_family_profile_results`) or add the key to the effect list in the D2 change, empty when `selected_profile_id` is non-null.
3. **BLOCKER** `lib/canonical-v2/m7-v2-deterministic-generator.js:2025, 2067, 653-660`, `scripts/stage-2y-structure-m7-v2-repair-work2-real-text-lib.mjs:330` (D1-generator:F3, D2-contract:07). A claim naming two M2 nodes keeps `source_node_occurrence_ids[0]` only: the second node's bytes appear in no span, closure, coverage entry or attempt record, while the closure asserts `COMPLETE_REVIEWED_SOURCE_CLOSURE` and the `MATERIAL_SPAN_UNMODELLED` issue's `source_span_ids` names node[0]'s span, which already carries `REVIEW_RESIDUE`. Measured: 55 multi-node claims, 48 with dropped nodes not byte-contained in node[0], 12,805 bytes of CHAPEAU, LIMB and QUALIFICATION text discarded (08fd217e…, claim a2c6c573…: node[0] LIMB 31375-31564, dropped CHAPEAU 31187-31289). Spec D1(d), D2(e), re-plan §4.2 and §4.5, Decision 26 Q16. Ruling: do not restore the retired "two nodes on one claim" throw. Project a hash-bound `context_spans` entry for each named node that is the governing ancestor; otherwise emit `MATERIAL_SPAN_UNMODELLED` whose `source_span_ids` names a span over that node's exact extent, or `DEPENDENCY_UNRESOLVED` with both candidate spans, and record every named node id in the attempt record.
4. **BLOCKER** `tests/stage-2y-structure-m7-v2-repair-contract.test.js:9043-9118` (D1-generator:F4). All three new generator cases, including the replacement for the retired shared-node throw at :9085, assert only generator output and never call `validateAnalysisV2`, unlike every other generator golden case in the file (:8530-8533, :7951-7954). The retired case failed closed; its replacement passes on output the contract rejects. Spec D1(a), Acceptance. Add `assert.equal(validateAnalysisV2({analysis: generated, resolveBinding: fixture.resolveBinding}).status, 'PASS')` to all three.
5. **BLOCKER** `scripts/stage-2y-structure-m7-v2-repair-work2-real-text-lib.mjs:318-323` (D3-scripts:1, run-verification:1). The governance object carries 4 of the 9 keys `validateGovernance` requires (`m7-v2-contract.js:1651-1664`), missing `candidate_registration_verification`, `candidate_registration_binding`, `code_bindings`, `view_policy_binding` and `predecessor_receipt_bindings`, and shapes `semantic_input_bindings` as `{input_role, binding}` where the contract requires `{role, binding}`. `generateAnalysisV2` clones it unchecked, so `run` succeeds and `validate` fails on shape for all ten before the lifecycle state is consulted; `run.mjs:59-61`'s `governance_check` is a bare string comparison. Spec D3, Acceptance, `real_agreement_receipt_guard.validator`. Build all nine fields as `w5-dryrun-dedupe.mjs:30-44` does; every missing value was available pre-manifest (the Work 4 successor manifest at base carries a `PASS_CANDIDATE_REGISTRATION` verification for candidate `9a3ccbf7…`). Completed, the run reports `M7_V2_BINDING_DRIFT` on the `DETERMINISTIC_GENERATOR` binding, which is the real pending signal and mine to clear. Add a test pinning the resulting failure code.
6. **BLOCKER** `lib/canonical-v2/m7-v2-deterministic-generator.js:2186-2204` (D1-generator:F5, scope-discipline:F1). `makeFact` is still called with `legalSubject: 'COMPANY'` and the APPLIES_TO draft is still built from projected party edges; the diff only converted `projectPartyEdges`' BOUND_ENTITY/RESOLVED `fail()` into a silent filter (:387-392). The path is unreached only because 0 of 2,101 occurrences matched a single profile, and the test at :8386-8535 keeps it green. Spec D1(c), re-plan §4.6. Delete the constant and the invented draft, and project party edges only from the party context-disposition record named in the authority (`context_disposition_records[0]`), leaving Phase 1 with no party edges.
7. **BLOCKER** `lib/canonical-v2/m7-v2-contract.js:12066, 12327` (D2-contract:04). Both issue-code sites accept any non-empty string; there is no vocabulary and the diff adds none. Spec D2(b). Add a `REVIEW_ISSUE_CODES` constant with the six codes and validate `issue.issue_code` and `rule.validation.issue_codes` against it, so an unknown or missing code fails rather than passing as free text.
8. **BLOCKER** `scripts/stage-2y-structure-m7-v2-repair-work2-real-text-lib.mjs:357`, `lib/canonical-v2/m7-v2-contract.js:7865` (D2-contract:05, scope-discipline:F2). `definition_resolution` is hardcoded to `{rule_id: null, unresolved: true, candidates: []}` for every occurrence; `RULE_1_DEFINITIONS_ARTICLE` and `RULE_2_PREAMBLE` appear nowhere in `lib/`, `scripts/` or `tests/`; `DEPENDENCY_UNRESOLVED` is never emitted. The M3 data is present and unread (one agreement carries 2,334 definition edges and 275 reference edges). Spec D1(d), D2(d), D4, re-plan §4.2 and §5 Phase 1 R2. Not deferred: implement the projection with the resolution rule id recorded, rule 3 only for byte-identical candidates after whitespace normalisation, and the unresolved fallback carrying both candidate spans.
9. **BLOCKER** `lib/canonical-v2/m7-v2-deterministic-generator.js:568` (scope-discipline:F3). `context_spans` is hardcoded `[]` on the only path all 2,101 claims take, so the validator side added in this diff is never exercised and no PARENT_CHAIN, GOVERNING_CHAPEAU or ARTICLE_CHAPEAU span is ever derived. Spec D2(c), re-plan §5 Phase 1 R2, which puts hash-bound closures with the ancestor-context extension in Phase 1, not Phase 2. Populate it from named ancestor nodes, hash-bound against sealed M2 bytes. Your D4 note says "when D2 is wired"; wire it.
10. **BLOCKER** `scripts/stage-2y-structure-m7-v2-repair-work2-real-text-lib.mjs:379-396` (scope-discipline:F6). `reasonCodeCounts` is one flat map with no family axis, and it sums `disposition.issues[]` and `coverage_partitions[].entries[]` into the same key space, so the committed report shows `NO_SINGLE_PROFILE: 4202` as one undifferentiated number. Spec D3 ("the per-family report ... broken down by reason code"), re-plan §4 false-complete guard. The axis is free: every M4 claim you already read carries a populated `family` field, dropped before the report is built. Group by family and keep the two axes separate in the artefact, not only in the Q.
11. **BLOCKER** `scripts/stage-2y-structure-m7-v2-repair-work2-real-text-lib.mjs:165` (scope-discipline:F8). `ledgerEntriesForText` scans modal tokens and proviso markers only and never enumerates M2 children by kind, so the limbs component of `ledger_entries` is never populated though the D4 note describes it. Spec D4, A-0020 Q-0018. Enumerate `AUTHORED_INLINE_LIMB`, `LIMB`, `QUALIFICATION` and `CHAPEAU` children via `parent_node_occurrence_id`.
12. **BLOCKER** `scripts/stage-2y-structure-m7-v2-repair-work2-real-text-lib.mjs:51-53, 193` (scope-discipline:F9). `party_candidates_with_spans` is a scan against a generic five-token `PARTY_SEEDS` list, not the Q-0021 tables: 35 rows, 1,638 sha-verified spans, agreement-specific disambiguation, and a vocabulary including `OpCo`, which your seed list cannot match. Neither the Q-0021 output path nor its hash appears in the PR, yet the D4 note claims "recorded from the Q-0021 table". Spec D4. Bind the Q-0021 table, record from it, and correct the note.
13. **BLOCKER** `scripts/stage-2y-structure-m7-v2-repair-work2-real-text-lib.mjs:480-491` (D3-scripts:4). `buildReceipt` recomputes `agreement_ids`, `analysis_count` and `disposition_count` but copies `reason_code_counts` and `report_sha256` verbatim from `report.json`, and neither `finalise` nor `validate` rehashes that file, so a hand-edited or stale report seals into the receipt unnoticed. Spec D3, `real_agreement_receipt_guard.receipt_counts_recomputed_from_bound_files`. Call the exported `reasonCodeCounts(analyses)` and recompute the report's content hash from the bound analysis files, failing on mismatch.
14. **BLOCKER** `tests/stage-2y-structure-m7-v2-repair-work2-real-text.test.js` (D3-scripts:5, scope-discipline:F7). The file has three cases; the fifth named behavioural case, re-run is byte-identical, is absent and nothing else in the diff covers it. The behaviour is currently correct, so this is a coverage gap on a named acceptance item. Spec D3 test list. Add a test that runs the pipeline twice on the same fixture inputs and asserts byte or canonical-JSON equality, as `tests/canonical-v2-native-producer-determinism.test.js` already does.
15. **CHANGE** `lib/canonical-v2/m7-v2-deterministic-generator.js:571` (D1-generator:F6). A multi-node claim enters the review-only path even when a unique most-specific profile was selected, and `profileMatchState(profileResults, null)` then reports `AMBIGUOUS_PROFILE_MATCH` with `NO_SINGLE_PROFILE`, which D1(b) reserves for zero or several matches; the contract recomputes the state from the effect matches, so this fails the profile gate the moment a profile matches. Pass the selected profile through, suppress `NO_SINGLE_PROFILE` when one was selected, and route the multi-node case on a source-side issue code alone.
16. **CHANGE** `lib/canonical-v2/m7-v2-contract.js:7873` (D2-contract:08). The new context-span check proves the quoted bytes hash to the recorded digest but never resolves `node_occurrence_id` against the index, never requires it to be an ancestor of the closure's source node, and never ties the byte range to that node's extent, so a span may name any string and quote any range. Spec D2(c). Resolve the id, require ancestry, require containment in the ancestor's extent, and require the kind to match the ancestor's role.
17. **CHANGE** `lib/canonical-v2/m7-v2-contract.js:7865` (D2-contract:10). Neither new contract branch has a test: `context_spans` and `REVIEW_RESIDUE` appear zero times in the contract test file, and both are unreachable through the shipped path. Add a closure with a real non-empty `context_spans` entry (pass, and hash-corrupted fail) and a coverage partition with a `REVIEW_RESIDUE` entry (pass on a review-only closure, fail on a NORMAL rule's closure and on a closure carrying both).
18. **CHANGE** `scripts/stage-2y-structure-m7-v2-repair-work2-real-text-lib.mjs:81` (D3-scripts:6). `parseArgv` sets `options.outDir` and no script reads it; `validateOutputs` has an `outRoot` parameter `validate.mjs` never passes, so `--out-dir` is silently ignored. Wire it through or remove the flag.

19. **CHANGE** `lib/canonical-v2/m7-v2-deterministic-generator.js` (run-verification:5, lead ruling). The ten analyses are 118 to 307 MB each, 1.8 GB in total, because every inspected effect carries `profile_results` for all 1,382 profiles of the bound set. That cannot be committed and the receipt must bind committed files. Scope `profile_results` to the profiles of the occurrence's own family (the M4 claim's `family` field, which you already read) and record the bound profile-set digest once per analysis in place of the other families' results; the contract's completeness check on `profile_results` becomes "every profile of the occurrence's family", which I authorise as a contract change in the authority correction below. Report per-agreement file sizes in Q-0025; the ten together must be under 200 MB or say why not.

# Not required, noted

- D1-generator:F7 `DEPENDENCY_UNRESOLVED` never emitted (generator:534): subsumed by item 8.
- D1-generator:F8 the synthetic first-slice compiler survives as the only non-review path and aborts the agreement (generator:2094): tracked for Phase 2, when a real non-review path exists.
- D1-generator:F9 `considered_family_keys` carries the bound set's 24 families where the contract requires 25 (generator:604): resolves with the interim registration.
- D2-contract:11 `REVIEW_RESIDUE` `reason_code` is unconstrained free text with no authority and no tie to the disposition's issue codes (contract:11024): folds into item 7's vocabulary.
- D2-contract:12 test census: 34 to 30 required-throw cases, 70 to 73 tests, no other assertion altered (contract test:8543): recorded as this package's sealed-test delta.
- D3-scripts:7 dead `WORK3_ANALYSIS_SET`, `WORK3_INDEX_SET`, `WORK3_CONTEXT_SET` constants (lib.mjs:30): tidy when convenient.
- run-verification:3 the authority's `exact_argv` for `run` omits the `--registration` flag the script and D3 require: authority drift, mine to correct.
- run-verification:4 two attempt-record member names differ between the authority's list and the actual D4 field names: authority shorthand drift, mine to correct.
- ci-red:002 the phase-1 scanner's outcome for your new scripts is unconfirmed in that CI run (the other shards were cancelled): resolved by the integration action below.

# Refuted by verification

- D2-contract:01 REVIEW_RESIDUE guard tests one arbitrary rule: the generator cannot produce the mismatched-disposition input, and validation throws earlier on item 1.
- D2-contract:06 PAGE_NUMBER SOURCE_ARTEFACT coverage missing and REVIEW_RESIDUE opening a route past the artefact proof: the re-plan scopes the effect ledger to Phase 2, and the review-only confinement check holds.
- D2-contract:09 context spans not admitted into matcher context or fact provenance: Phase 2, and the authority's guard requires only hash verification.
- D3-scripts:2 bindings never checked against git HEAD: `validateAnalysisV2` already cross-checks M2, M3 and M4 against the sealed sets by sha256 and git blob oid; only the literal "Work 7 resolver" wording is unmet.
- D3-scripts:3 validate never checks the attempt records: A-0017 moves the attempt-record recompute to an internal agent; you build.
- run-verification:2 phase-1 scanner fails with the new scripts unclassified: expected, the file is outside your write exceptions, and you supplied the category as asked.
- run-verification:5 analysis files are 118-307 MB because of per-occurrence `profile_results`: that field is required by the sealed candidate-effect `exactKeys`; dropping it would break the contract test file.
- scope-discipline:F4 SOURCE_ARTEFACT coverage absent: it exists at base (`m7-v2-contract.js:7529-7544, 10988-11061`) and you correctly left it alone.
- scope-discipline:F5 `run` bypasses `consolidateAnalysis`: calling it literally would abort every agreement on the interim registration, contradicting the Acceptance text.
- ci-red:001 `CANDIDATE_BINDING_DRIFT` on the Work 4 test: an authorised consequence of D1, mine to clear, not a change to this diff.

# Integration actions on the lead's side

- Classify the new scripts in `lib/canonical-v2/phase1-authority-boundary-inventory.js` as `LOCAL_ARTIFACT_WRITER`, the category you proposed, and add the matching phase allowlist and phase-1 inventory entries so `tests/canonical-v2-phase1-authority-boundary.test.js` stops reporting `UNCLASSIFIED_CHANGED_SOURCE`. Do not edit that file yourself.
- There are four new files, not three: the shared helper `…-work2-real-text-lib.mjs` needs an entry alongside `run`, `finalise` and `validate`. Confirm in Q-0025 that `LOCAL_ARTIFACT_WRITER` is your intended category for the helper too.
- Land the count-unpinning commit and issue the registration binding the changed generator bytes, clearing `CANDIDATE_BINDING_DRIFT` on the Work 4 test and the `M7_V2_BINDING_DRIFT` item 5 will surface. A later A names the commit and the interim registration path (A-0018 was reused for the cadence note; ignore A-0017's pointer to it).
- Correct the authority's `exact_argv` and `attempt_record_members` drift (notes 7 and 8 above) and add the family-scoped `profile_results` contract change (item 19) to `contract_changes_authorised`, as one additive correction record.

# Next Q

Resubmit as Q-0025 on the same branch and the same PR (#489), with the reason-code counts from the re-run, the per-family breakdown from item 10, and the helper's boundary category. No round limit.
