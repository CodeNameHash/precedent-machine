# Handoff, 6 August 2026

Single-use document. It tells the next session where things stand and what to
do next. It does not restate the underlying documents; it points at them.
Delete it once acted on.

Branch `codex/m3-production-phase1`. Suite green at 7,718 tests, 0 failing.
Build clean. Twelve commits ahead of `main`, which last merged at `016288cb`.

---

## Read these first, in this order

Thirty minutes here saves hours. This session repeatedly rebuilt or
re-derived things that already existed, and every one was findable.

1. **`OPERATING-RULES.md`.** What you are and are not permitted to do, and the
   working conventions. Read the authority boundary at the top in full. Note
   that importing canonical data became permitted on 6 August, while production
   data access and real credentials did not.
2. **`ROADMAP.md`.** The plan, in three lanes: P for product, S for security, D
   for delivery. This is the document that says what to do and in what order.
3. **`DECISIONS.md`.** Decisions already taken, with the reasoning. Read before
   proposing anything that sounds like a fresh design choice; it is probably
   already decided here.
4. **`git log --oneline origin/main..HEAD`, then the full messages.** The most
   reliable record in the repository. Written to be read.
5. **`notes/doc-reality-audit.md`.** Twenty-eight ways the documents and the
   system had drifted apart. Read it before trusting any document not listed
   above.

Then read the note matching whatever you are about to work on. There are
twenty-five under `docs/codex-program/notes/`, one per piece of work, each
recording what was proved versus judged and what was deliberately left alone.

## What already exists that you will probably assume does not

This is the list I wish I had been given. Each of these cost real time this
session, and in three cases I told the owner something was missing when it was
sitting in the repository.

- **A general extraction runner.** `scripts/canonical-v2-live-extraction-run.mjs`
  runs any registered family against any pinned deal, with a `--dry-run` mode
  that resolves sections and reports projected cost without calling a model. I
  concluded no such runner existed and nearly built a second one.
- **Twenty-five registered section families**, each with its own producer
  prompt, resolved through `getProducerPromptModule()` against
  `listRegisteredSectionFamilies()`. I said extraction was not available for
  families beyond the one I was working on. It was, for all of them.
- **The per-family assets**: 28 producer prompts, 16 product projections, 26
  review table configs, 27 resolver handlers, 4 dark bridges. These live under
  `lib/`, not under `scripts/`. Reading only `scripts/` gives the false
  impression that per-family work does not exist.
- **A complete database schema and writer for canonical data.**
  `supabase/canonical-v2-foundation.sql`, 8,686 lines, with content-addressing
  functions and a `canonical_v2_write` function. Never executed; its tests
  match text patterns against the SQL source. I told the owner no path from
  extraction to the database existed.
- **An equivalence harness.** `scripts/review-parity-check.js` compares the V2
  view of a family against the legacy view, deal by deal, reading files only.
- **A section-family classifier** that can assign families to sections
  automatically. Deliberately not used for families we want served, because
  anything it classifies carries a blocking unverified flag.
- **A parity register that re-derives its own status** rather than storing it,
  in `lib/canonical-v2/native-producer/m3-family-parity-register.js`. It is the
  best model in this repository for a check that cannot go stale, and the
  documentation audit recommends copying its shape.

---

## The one thing to do next

Re-run all 25 families twice: against Modiv, and against TopBuild.

They answer different questions and both are needed.

**Modiv is the controlled comparison.** Same agreement, same sections, measured
against the pinned baseline at
`docs/codex-program/notes/all-families-baseline-20260806.json`. Every
difference is attributable to a fix landed this session. This is what tells you
whether the work worked.

**TopBuild is the honesty check.** Almost every fix this session was tuned on
Modiv's drafting: the synonym lists learned Modiv's vocabulary, the termination
fix parses "from X to Y" because that is how Modiv's limb heads read, and the
party-capacity entry exists because Modiv is an UPREIT. A different drafter
breaks different assumptions.

TopBuild is close to an ideal contrast rather than just a second sample. It is
a financed deal where Modiv is not: 39 mentions of Debt Financing against
Modiv's zero, a financing condition, and dedicated sections at 4.17 "Financing
Provisions" and 7.16 "Waiver of Claims Against Financing Sources".

That matters because it tests a conclusion reached this session. Guaranty
returning zero on Modiv was judged correct, on the grounds that an unfinanced
REIT merger has no financing-party protections to find. **TopBuild should make
that family produce real data. If it returns zero there too, that reasoning was
wrong and the family is broken rather than correctly quiet.**

### How to run it

`scripts/canonical-v2-live-extraction-run.mjs`, one process per family, in
parallel. Six to ten at once worked without throttling. `--dry-run` resolves
sections and reports projected call count without calling a model, so map
families to sections for free before spending anything.

Modiv's family-to-section mapping is in the commit for `4788860b` and in each
evidence directory's run manifest. **TopBuild's mapping does not exist yet and
must not be assumed from Modiv's**: TopBuild has 63 sections to Modiv's 99 and
its articles do not align, Modiv's termination sitting at 7.1 while TopBuild's
boilerplate runs to 7.16.

Expect roughly 70 model calls and $25 for Modiv. The first sweep measured 58
calls and $20.30 for 25 families; citation-following is now on by default but
is scoped to termination-fee bare-citation triggers and adds nothing elsewhere.
TopBuild is an estimate, not a measurement: perhaps 50 to 60 calls.

Capitalisation was the outlier at 18.8 minutes and timed out on its second
call. Use `--call-timeout-ms` above the 600,000 default for that family.

---

## A restructure the owner asked for, not yet done

Move the core documents into their own folder and cross-reference them, so
that reading that folder alone tells you everything. It was not done in this
session only because two agents were writing to those files at the time.

The analysis is done and the constraints are narrower than they look.

**No markdown document is read at runtime.** Every apparent reader is a comment
mention. `ROADMAP.md` looks like it is read by eleven files and is read by
none. Verified by separating genuine reads, meaning a path passed to a file
read or resolve, from mentions in prose. This distinction has burned this
project before, so re-verify rather than trusting this paragraph.

**Four documents record their path in `docs/codex-program/specification-manifest.json`**
and are byte-pinned there: `docs/CODEX-PROGRAM.md`,
`docs/codex-program/EXECUTION-LEDGER.md`, `canonical-contracts.md` and
`adversarial-tests.md`. Moving any of them means updating its recorded path in
the manifest, in the same change, and confirming
`node scripts/verify-codex-program-spec.mjs` still passes.

**Two pinned files are data, not documents, and are genuinely read by code**:
`programme-gates.yaml` and `m3-family-parity-register.json`, by
`lib/programme-gates/governing-registry.js` and
`lib/canonical-v2/native-producer/m3-family-parity-register.js` among others.
Leave those where they are. They are pinned alongside the documents for
historical reasons but they are not part of this restructure.

**Comment references in `lib/` should be corrected** for accuracy even though
nothing breaks if they lag. Nothing depends on them.

The goal is a reader who opens one folder and can get current without
exploring: an entry point that says what to read in what order, then the
operating rules, the roadmap, the decision log, the codebase guide, the
graveyard, and this handoff, each linking to the others rather than repeating
them. The reading order and the already-exists list in this document are the
starting point for that entry page.

---

## What landed this session

Read the commit messages; they are written to be read and are the primary
record. `git log --oneline origin/main..HEAD`.

The largest pieces, each with a note under `docs/codex-program/notes/`:

- A quote trim that could drop a governing negation, inverting a materiality
  representation, was live in the production ingestion gate with no boundary
  check at all, and two committed tests named KNOWN LIMITATION pinned it as
  accepted. Now blocked. See `negation-reversal.md`.
- All 25 families run live for the first time. See `all-families-aggregate.md`,
  its adversarial review in `all-families-aggregate-review.md`, and
  `open-world-ownership.md`.
- Termination corroboration anchored on the limb that grants the right, taking
  that family from 1 resolved claim to 8. See `resolver-reference-fixes.md`.
- Three extraction crashes, one a frozen array latent in all 25 families. See
  `extraction-crashes.md`.
- Two sectionizer defects: whole sections lost to a title-length cap, and 112
  references broken because the letter after "z" had no successor. See
  `swallowed-headings.md` and `nested-lettering-collision.md`.
- P1 completed for termination fees: a conditional fee now resolves to both its
  amounts with the grounds that trigger each.

---

## What to be careful about

**The documents have drifted from the system repeatedly, in both directions.**
`docs/codex-program/notes/doc-reality-audit.md` records 28 findings, and a
separate agent has been reconciling the tracking documents against it. Trust
the commit messages and the code over any document that has not been checked.

**One error class has produced three separate confident false findings**: using
a JavaScript string index, which counts UTF-16 code units, against a UTF-8 byte
offset. This pipeline slices text by bytes everywhere. A conversion helper
exists. Any probe using `indexOf` or `slice` against a stored offset will
produce a plausible wrong answer.

**A zero from a family is not necessarily a failure.** Guaranty on Modiv
returns nothing because the agreement genuinely lacks those provisions.
Treating that as a defect means inventing provisions the deal does not have.

**Never pipe `npm test` into `tail` or `head`.** The pipeline returns the last
command's exit code and will report a passing suite when it failed. Redirect to
a file, echo `$?`, then grep.

---

## Known and not fixed

- **Appraisal** produces nothing because a flat "appraisal rights are not
  available" clause appears to have no slot in that family's schema. A
  vocabulary question, not a bug.
- **Six of the twelve** termination candidates now clear the party gate and
  stop at a different one, on four identified vocabulary gaps.
- **The negation fix's principled form**, capturing the offset before any trim
  rather than re-deriving it after, is specified in `negation-reversal.md` but
  not implemented.
- **A third bridge** rejected the negation fix for a genuine reason; the
  false-positive shape is written up, the fix is not built.
- **The party-capacity list assumes one party per string**, and the corpus
  contains joint obligations naming five at once.
- **Model responses can arrive as prose**, not JSON, when the model goes
  agentic under the command-line transport. A failed run now keeps a partial
  receipt, but the behaviour will recur.

---

## Open questions for the owner

1. **Does `public.canonical_v2_write` work?** 8,686 lines of committed SQL
   schema and writer that has never been executed; its only tests match text
   patterns against the source. See `import-plan.md`.
2. **What do the open-world objects feed on the product side, if anything?**
   Marked unconfirmed in the import plan's mapping table.
3. **Whether to run the import against a real database**, which the plan gates
   as a separate authorised act. Import itself is now permitted; production
   data access and real credentials are not. See `OPERATING-RULES.md`.

---

## What has not moved

Nothing extracted this session reaches the product. Extraction writes JSON into
`evidence/`; the site reads Postgres; nothing writes canonical data to Postgres.
The schema for it exists and has never run. Parity blockers stand at 102, down
from 104.

The pipeline is proven across families on one agreement. It is not connected to
anything a user sees, and the next session should not read the volume of work
here as meaning otherwise.
