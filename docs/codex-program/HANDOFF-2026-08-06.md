# Handoff, 6 August 2026 (v2, consolidation pass)

Single-use. Delete once acted on.

Branch `codex/m3-production-phase1`, merged into `main` at `20f09550`. Pull
locally with `git checkout main && git pull`.

## The job for this session

The core documents exist but have not been checked as a set. Read them, fix
what is wrong, and make sure a coding agent could pick up `docs/core/PLAN.md`
and execute without rediscovering anything. The owner also wants to be able
to read them and understand the project.

Start at `docs/core/README.md`. It gives the reading order:
`OPERATING-RULES.md`, `PLAN.md`, `COMPLETED.md`, `DECISIONS.md`,
`CODEBASE-GUIDE.md`, `GRAVEYARD.md`.

## Known problem, not yet fixed

`PLAN.md` line 145 says the section-family classifier is "deliberately not
wired in" and that anything it classifies "carries a blocking unverified
flag". Both are false. It is wired in as an opt-in parameter to
`runNativeExtraction`, exercised by dozens of tests, and the blocking flag
fires only for its model-assisted tier; deterministic rule matches carry no
flag. Twenty-six families of automatic section routing exist today, at zero
model cost. Step 2A's premise, that 24 of 25 families need their sections
recovered by hand, may not survive this. Check it and fix the plan
accordingly.

Full detail: `docs/codex-program/notes/module-reads/module-read-aa.md`,
section "HEADLINE FINDING".

## Unread material, ready to fold in

Five agents read 156 source modules and audited their header comments for
the same class of error (a header asserting something the code no longer
does). All five were killed mid-task by an infrastructure outage, not by
failure; each had written incrementally, so their reports are intact and
committed, but **none of them produced a final summary**, because the outage
cut them off before that step. Read the reports directly:

- `docs/codex-program/notes/module-reads/module-read-aa.md`
- `docs/codex-program/notes/module-reads/module-read-ad.md`
- `docs/codex-program/notes/module-reads/header-audit.md`
- `docs/codex-program/notes/module-reads/header-audit-ab.md`
- `docs/codex-program/notes/module-reads/header-audit-ac.md`
- `docs/codex-program/notes/module-reads/old-docs-sweep.md`

Two batches of modules (roughly 78 of 156) were never assigned to an agent at
all: batches were split four ways and only some completed. Check
`/Users/bengoodchild/.claude/jobs/57145eaa/tmp/hdr-batch-*` for the original
split if you need to see which files were covered.

This is where further "gold" is most likely: capability that already exists
and is being worked around, in the style of the classifier finding above.

## Standing instruction from the owner

Both registers, always: plain English a lawyer can read, and enough technical
detail (file paths, function names, commands) that a coding agent can act
without re-deriving anything.

## Deferred, do not do

A full archive sweep across every folder, not just the top level, was
requested and then explicitly deferred by the owner to later in the project.
Do not do it in this session.

## House style

No em-dashes or en-dashes. British spelling. Verify with `grep -c '—\|–'`.

## Verification

    CI=true npm test > /tmp/x.log 2>&1; echo "EXIT=$?"

Never pipe into `tail` or `head`. Suite is green at 7,724 tests as of the
merge commit.
