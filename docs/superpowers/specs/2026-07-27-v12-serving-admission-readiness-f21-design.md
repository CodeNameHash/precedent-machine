# V12 serving-admission readiness F21

## Purpose

Determine whether the cumulative V6 through V12 contract is ready for a
separate serving-admission decision. F21 records evidence and blockers only. It
does not add V12 to the serving allowlist, activate a feature, write corpus
data, change a release pointer or grant query, serving or publication authority.

## Approved legal decisions

Ben approved the following in the main Codex task on 27 July 2026:

1. The 23 atomic no-shop actions, their comparison roll-ups and the exact
   shared and information-specific exception prerequisites.
2. For the QXO copy obligation, receipt of each qualifying Company Acquisition
   Proposal or Company Request starts a separate primary 24-hour clock. The
   item-receipt reading remains a visible unresolved alternative.
3. The primary interpretation may support the distinct copy-delivery market
   metric only in an interpretation-keyed cohort with the alternative reading
   and lawyer warning retained on every surface.

The previously approved definition rules also remain binding: an ordinary
source-local plural is not a duplicate or permanent alias, nested definitions
are retained, and unresolved terms cannot be silently skipped.

## Attestation

F21 produces one deterministic `V12_SERVING_ADMISSION_READINESS_F21/V1`
attestation. It must:

- pin the exact V5 active-serving predecessor and V6 through V12 fingerprints;
- inventory the permitted cumulative contract delta for each version;
- bind the approved legal decisions to the governed version where they first
  appear;
- bind the exact F20 candidate and indexed-query certification;
- prove V12 remains absent from the active serving fingerprint set;
- prove the candidate retains no active serving, query or pointer authority;
- reject a missing intermediate version, unexpected contract delta, changed
  legal decision, drifted F20 evidence or widened serving allowlist; and
- return either `READY_FOR_SEPARATE_SERVING_ADMISSION_DECISION` or exact
  blocker codes.

The approval record is readiness evidence only. It is not a serving admission,
release activation or production-cutover authorisation.

## Acceptance

- The V6 through V12 chain is complete and immutable.
- Each version changes only its governed contract area.
- The F20 attestation is exact and independently validated.
- The three approved legal decisions and definition rules are explicit.
- V12 remains outside `FIXTURE_SERVING_CONTRACT_FINGERPRINTS`.
- Canonical Query and market statistics remain feature-gated closed.
- No database command, release mutation or activation path exists in F21.
- Full tests, production build, phase allowlist and independent architecture
  and legal-semantic reviews pass.
