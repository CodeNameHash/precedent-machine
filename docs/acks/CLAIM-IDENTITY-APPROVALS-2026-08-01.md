# Approvals — claim-identity slice, Ben, 2026-08-01

Recorded verbatim from the in-session decision round ("Agree with all
recommendations", 2026-08-01). Each item was put to Ben as an explicit
question with a recommendation; all five recommendations were approved.

1. **ACCURACY-code whitelist v1 phrases — APPROVED.**
   `MAT_MATERIAL_TO_COMPANY` ← "except as would not be material to the
   Company"; `MAT_MAE_QUALIFIED` ← "true and correct in all respects,
   except as would not reasonably be expected to have a Material Adverse
   Effect". Variants fail closed to review (the whitelist's growth intake).
   The in-file DEVIATION flags in `qualifier-kind-lexicon.js` are resolved
   by this approval.

2. **Bring-down tier provenance — APPROVED option (b).** Keep MECHANICAL,
   but the tag's pins must name the gate explicitly
   (`gate: 'ALLOWED_VALUES_MEMBERSHIP'`) so the tag never overstates how
   the value was produced. Revisit before auto-pass opens for the family.

3. **Next-slice priority — APPROVED (a) then (c).** (a) mint
   `PROVISION_COMPONENT` rows for assertion-node subjects so limb-level
   claims can publish (closes final-audit M1); then (c) breadth — a second
   deal's capitalisation rep to exercise drafting variance and start the
   commonality evidence loop. (b) producer attachment iteration rides with
   the next prompt change.

4. **Symbolic dates "the Closing Date" / "the Effective Time" — APPROVED
   to leave abstaining** (open world) until the enrichment/deal-data slice
   supplies a governed source for their values.

5. **Pull request — APPROVED to open now**, with PR-activity watching for
   CI and review comments.

Standing items this approval round does NOT cover (already governed by
spec, in flight): KNOWLEDGE+TEMPORAL date preserved in the knowledge
claim's attributes; corpus-ruling measurement-date reachability computed
by rule; the RESOLUTION_REVIEW_QUEUE/V1 writer; wiring coverage proxies
and the enumeration scan into the run receipt.
