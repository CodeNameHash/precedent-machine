#!/usr/bin/env node
import { existsSync, readFileSync, writeFileSync } from 'node:fs';
import { dirname, resolve } from 'node:path';
import { fileURLToPath } from 'node:url';
import { createRequire } from 'node:module';
const require = createRequire(import.meta.url); const { collect, digest } = require('../../lib/canonical-v2/open-world-promotion-candidates');
const root = resolve(dirname(fileURLToPath(import.meta.url)), '../..'); const json = (path) => JSON.parse(readFileSync(path, 'utf8'));
const manifestPath = resolve(root, 'evidence/canonical-v2/open-world-promotion/cohorts/corpus-review-20260809.v1.json');
const manifest = json(manifestPath);
const loadReceipt = (receipt) => { const dir = resolve(root, dirname(receipt.resolution_path)); const resolutionPath = resolve(root, receipt.resolution_path); const adapterPath = resolve(dir, 'adapter-result.json'); if (!existsSync(resolutionPath) || !existsSync(adapterPath)) throw new Error(`MISSING_RECEIPT_FILE:${receipt.run_id}`); return { resolution: json(resolutionPath), runReceipt: json(resolve(dir, 'run-receipt.json')), source: json(adapterPath).admitted_source_contexts?.[0], resolution_sha256: `sha256:${require('node:crypto').createHash('sha256').update(readFileSync(resolutionPath)).digest('hex')}`, canonical_text_sha256: json(resolve(root, receipt.source_reference_path)).canonical_text_sha256 }; };
const result = collect({ cohort: manifest, loadReceipt, activeBundle: { fingerprint: 'FIXTURE_CONTRACT_INPUT/V31', mapping_table_version: 21 } });
const outDir = resolve(root, `evidence/canonical-v2/open-world-promotion/candidates/${result.cohort.digest.slice(7)}`); const out = resolve(outDir, 'candidate-set.v1.json');
if (process.argv.includes('--write')) { require('node:fs').mkdirSync(outDir,{recursive:true}); writeFileSync(out, `${JSON.stringify(result,null,2)}\n`); writeFileSync(resolve(outDir,'report.md'), `# Open-world promotion candidates\n\nAll candidates are held.\n\n- Native rows: ${result.counts.native_open_world_occurrences}\n- Raw recurrence: ${result.counts.raw_recurrence_labels_at_threshold} labels in three or more deals\n- Eligible recurrence: ${result.counts.eligible_concepts} concepts, ${result.counts.eligible_occurrences} rows\n- Resolved-claim duplicates: ${result.counts.resolved_claim_duplicates}\n- Later same-run identical duplicates: ${result.counts.later_same_run_identical_duplicates}\n- Fragment exclusions: ${result.counts.fragment_exclusions}\n\n## Diagnostic discrepancy\n\nThe written diagnostic census (77 resolved-claim duplicates, 762 fragments, 21 concepts and 146 rows) does not reproduce from its documented rules and the pinned receipts. The collector does not fit a rule to retain that census.\n\n- The documented 40-character normalised containment rule also matches topbuild TERMINATION, run \`topbuild-termination-20260809-2xk-r3-final\`, excerpt \`0181e332c8e9ba4a743f5c7a6014e9b9c0e77ab93c78f3a18f86536ca11db3f8\`: \`the required vote at the Parent Stockholder Meeting\`. It has 44 normalised characters. This changes the resolved-claim duplicate count from 77 to 78 and, by precedence, removes one fragment.\n- The documented fragment rule does not include five complete, upper-case clauses previously caught only by unrecorded prefixes: metsera EMPLOYEE_MATTERS \`Nothing in this Section 6.04 ...\`; concho EMPLOYEE_MATTERS \`Nothing in this Agreement shall constitute ...\`; concho EMPLOYEE_MATTERS \`Nothing in this Agreement is intended ...\`; skywater MISC_BOILERPLATE \`This Agreement (including the Exhibits and Schedules hereto) ...\`; modiv MISC_BOILERPLATE \`This Agreement (including the exhibits, schedules and other documents delivered pursuant hereto) ...\`. They change the fragment count from 762 to 756 after the duplicate precedence above.\n- \`AFFIRMATIVE_COVENANT\` occurs in six raw-deal records and has one remaining eligible row. No committed cluster rule identifies it as broad or permits its name-based exclusion. It remains in the report, producing 22 concepts and 147 rows.\n`); }
else if (process.argv.includes('--check')) { if (!existsSync(out) || readFileSync(out,'utf8') !== `${JSON.stringify(result,null,2)}\n`) throw new Error('STALE_CANDIDATE_SNAPSHOT'); }
else process.stdout.write(`${JSON.stringify(result.counts)}\n`);
