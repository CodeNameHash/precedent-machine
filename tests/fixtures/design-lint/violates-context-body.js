export function ViolatesContextBody() {
  const sectionNumber = '5.01';
  const section_number = '5.01';
  const provisionLabel = 'Interim Operating Covenants';

  return (
    <div>
      <span>{sectionNumber}</span>
      <span>{section_number}</span>
      <span>{provisionLabel}</span>
    </div>
  );
}
