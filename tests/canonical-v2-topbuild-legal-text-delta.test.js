'use strict';const assert=require('node:assert/strict');const test=require('node:test');const {buildTopBuildLegalTextDeltaPacket}=require('../lib/canonical-v2/topbuild-legal-text-delta');const build=(a,b)=>buildTopBuildLegalTextDeltaPacket({left:{occurrence_id:'A',canonical_text:a},right:{occurrence_id:'B',canonical_text:b}});test('keeps legal word number and punctuation changes blocking',()=>{for(const [a,b] of [['Buyer shall pay.','Seller shall pay.'],['Buyer shall pay 10.','Buyer shall pay 11.'],['Buyer shall pay.','Buyer shall pay;']]){const p=build(a,b);assert.equal(p.comparison.blocking_status,'LEGAL_TOKEN_DELTA_BLOCKING');assert.ok(p.legal_token_deltas.length);}});test('only states token match after declared presentation normalisation',()=>{const p=build('Table of Contents\n1\nBuyer shall pay.','Buyer   shall pay.\nEXHIBIT 2.1');assert.equal(p.comparison.normalised_token_sequences_match,true);assert.equal(p.comparison.raw_byte_equivalence,'NOT_ISSUED');});
