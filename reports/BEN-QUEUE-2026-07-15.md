# Ben decision queue — 2026-07-15

Every pending decision in one place. Each item: context → evidence → my recommendation →
the exact reply/command that executes it. Nothing here has been applied.

## 1. Mint 20 cards for card-less coded provisions
- **Context:** 20 provisions carry canonical codes but render nowhere (no card). The tool
  mints the card + its parser_regions anchor exactly as ingest would; post-mint invariant
  proves each pairs 1:1 with its source provision.
- **Evidence:** `reports/MINT-CARDS-DRYRUN-2026-07-14.txt` (per-provision table).
- **Recommendation: GO.** Safest destructive-adjacent item in the queue (insert-only,
  backed up, invariant-checked).
- **Execute:** reply "mint go" → I run `mint-cards.js --all --apply --backup ...`,
  rematerialize, live-verify.
- **Audit caveat:** minted regions carry synthetic offsets (0..len, flagged in metadata)
  — the full-document overlay cannot locate them in the source doc until M3 recomputes
  real offsets. Review-table rendering is unaffected.

## 2. Numeric normalization — one-line migration, then backfill
- **Context:** parser + backfill are built, tested (52 fixtures), and my 60-row audit of
  real parses was 100% correct (94% of 352 allowlisted claims parse; nulls honest).
  PostgREST can't run DDL, so the column needs the Supabase SQL editor.
- **Execute:** paste into SQL editor: `ALTER TABLE public.claims ADD COLUMN IF NOT EXISTS canonical_numeric jsonb;`
  then reply "numeric go" → I run the backfill (dry-run first) and report per-attribute counts.
- **Data flag found during audit:** at least one `noticePeriod` claim stores "24" (almost
  certainly hours) that would become 24 *days* — I'll exclude value>10 noticePeriod rows
  from the backfill and list them for review unless you say otherwise.

## 3. Safe Disclosure Carve-out — new category (taxonomy call)
- **Evidence:** `reports/SAFE-DISCLOSURE-SWEEP-2026-07-14.md` — pattern in 36/40 deals,
  101 rows; 45 merged inside the operative COR row, 56 scattered across 21 other
  categories; 5 deals have it only outside COR.
- **Recommendation:** create category **"Safe Disclosure Carve-out"** (NOSOL family) and
  recategorize the 56 scattered rows to it; leave the 45 merged-in-COR rows alone (the
  carve-out is part of their COR text). One flagged mis-classification (HireRight
  "Disclosure of Terms" row) gets fixed by the same pass.
- **Execute:** reply "carve-out go" (or amend the name) → I draft the decisions file,
  dry-run table to you, then apply.

## 4. forceTheVote hard/soft enum (codebook draft)
- **Evidence:** `reports/FTV-SWEEP-2026-07-15.md` — 10/40 deals have FTV language: 9 soft
  (self-termination-for-superior coexists), 1 hard (TopBuild — the corpus's only deal
  with no company self-exit).
- **Recommendation:** add `FTV_HARD` / `FTV_SOFT` / `FTV_NONE` codes on a new tagged
  scalar (or re-type `forceTheVote`), Fable-authored codebook, golden-eval-gated.
- **Execute:** reply "ftv go" → I author the rubric/codebook diff and run it through the
  golden eval before anything lands.

## 5. Regex-fallback deletion — not yet
- **Evidence:** `reports/REGEX-FALLBACK-MATRIX-2026-07-15.md` — 30 non-staging blocker
  cells; `parentAssignmentConditions` (14) and `changeOfRecommendationItems` (13) are the
  long poles; `governingLaw` is one deal short.
- **Recommendation:** HOLD until item 1 (mint) + a per-family review of the remaining
  cells distinguishes genuine absence from extraction misses. Re-run the matrix after the
  mint; genuine-absence cells can be declared confirmed.

## 6. TopBuild "duplicate" Solicitation Prohibition provisions
- **Evidence:** `reports/TOPBUILD-DEDUP-EVIDENCE-2026-07-15.md` — NOT duplicates:
  Company §4.3 vs Parent §4.4 mirror covenants (dual-vote merger).
- **Recommendation:** recat `b70adc75` → "Solicitation Prohibition (Parent)" (your
  approved convention) + mint its card; delete nothing. The corpus has 49 similar mirror
  pairs listed for the same treatment over time.
- **Execute:** reply "topbuild go".

## 7. Gap F — per-rep linkedBringDownStandard mis-stamp
- **Context:** 35/37 Metsera reps uniformly stamped MAT_MAE_QUALIFIED; the authoritative
  source is the closing-condition bringDownTiers; the render already derives from tiers.
- **Recommendation:** stop emitting the per-rep field at extraction (drop, don't derive)
  — one source of truth. **Execute:** reply "gap-f drop" (or "derive").

## 8. concept_key promotion — options memo
- (a) Keep `(type, category)` pair (today; works, label-fragile); (b) add computed
  `concept_key` column derived from the pair (cheap, stable joins, no semantic change);
  (c) full rubric-key promotion (heavy, best long-term).
- **Recommendation:** (b), as a mechanical additive migration when M4 query work starts.
  No action now.

## 9. Five staging deals (Summit, Noble Africa, Juniper, Catalent, Endeavor)
- All 5 fail canonical-rate; they're invisible to the product.
- **Recommendation:** re-ingest Juniper + Catalent + Endeavor (real deals worth having);
  retire Noble Africa (test artifact); Summit your call. Reply with any subset.

## 10. GAP-E residual semantics
- The plumbing shipped tonight, flag OFF, zero vocab. What remains is yours: whether
  extraction may return an explicit OTHER, the residual vocabulary names, and the
  freeze-gate loop policy. **Execute:** flip `RESIDUAL_CAPTURE_ENABLED=1` in Vercel env
  to see dimension C on /admin/schema-loss with zero write-side risk, any time.

## 11. New findings needing eyes
- **Landos parse regression:** stored full_text now parses to 6 sections (vs ~300
  provisions in DB) — a classify --apply would wreck the deal. Needs investigation
  before any classify pass. (`reports/COVERAGE-CLASSIFY-REPORT-2026-07-15.md`)
- **Cox document-type:** the stored document embeds a governance/stockholders agreement;
  classify wants §2–3 → OTHER. Decide whether such embedded agreements are in-scope.
- **Coverage% soft gates:** classify passes won't close them (structural). Consider
  lowering the 95% gate or fixing at ingest.
- **27 title-taken mint skips:** second provisions of already-carded concepts — mostly
  reciprocal mirrors; same (Parent) treatment as item 6 resolves them over time.

## 12. Housekeeping
- **Rotate the Supabase key** (legacy service_role JWT re-enabled + present in the chat
  transcript): Supabase → Settings → API — disable legacy, mint `sb_secret_`, put it in
  the claude.ai environment settings.
- The `structure-mechanics.config.js` effectiveTimeShort guard is now redundant (gap E
  verified fixed) — mechanical removal queued with the next config touch.

---

# Added 2026-07-15 from the root-and-branch code review (`reports/CODEBASE-REVIEW-2026-07-15.md`)

All findings below were independently verification-confirmed. The mechanical fixes are
already landed (QRY-1/QRY-3 query bugs, TAGS drift, dead-code/backup cleanup, a11y quick
wins). What remains here needs your judgment or a corpus-impact call.

## 13. Security — the biggest single item
- **No auth layer on any API route** (CRITICAL). 39 routes use the RLS-bypassing
  service-role client; `pages/api/users.js` self-grants `is_admin`; `pages/api/admin/
  reprocess-cond.js` is an unauthenticated destructive delete+reinsert. **First question:
  is prod behind Vercel deployment protection?** (I can't see that from the repo.) If yes,
  severity drops sharply. If no, the corpus is world-readable/writable.
  - **Recommendation:** confirm Vercel protection; if absent, add auth middleware gating
    all `/api/**` (a shared-secret header or Vercel auth) before anything else. Reply
    "security plan" and I'll draft the middleware + a route-by-route read/write/public
    classification.
- **SSRF** in `/api/ingest/from-url.js` (no scheme/host validation, follows redirects) and
  **no rate-limiting** on the LLM endpoints (budget DoS). Both fold into the same auth pass.

## 14. Code-correctness findings (verified) — disposition needed
- **EXT-1 alias-def carve-outs (HIGH):** 24/68 MAE definitions ship with no carve-outs (dead
  filter at `extract.js:4429`). Fix is mechanical + a targeted DEF re-extract. Reply
  "alias-def go" → I fix the filter, golden-eval it, and re-extract DEF.
- **EXT-2 quote-gate hole (HIGH, corpus-impact):** `verification.js:94` verifies a long
  quote on its first 80 chars, so a genuine head + fabricated tail passes the zero-flags
  gate. Tightening it could re-flag existing claims — needs a corpus QA-impact run before
  landing. Reply "quote-gate go" → I measure impact on all 40 then propose the fix.
- **EXT-3 provenance-offset corruption (HIGH):** `extract.js:3647` stamps chunk-first
  startChar on every provision in a multi-section chunk (88/108 groups affected). Touches
  extract core — spec + golden-eval before landing. Reply "offset go".
- **EXT-4 per-type-reprocess strip (HIGH, latent):** reprocessing REP or COND would strip
  MODEL_READONLY features unrecoverably. Did NOT fire on TASK 3. Fix = a guard that refuses
  (or post-passes) such a reprocess. **Do NOT run a REP/COND per-type reprocess until this
  guard lands.** Reply "reprocess-guard go".
- **EXT-5 dedupe-by-text drops second code (MED):** `store.js:496` dedupe ignores code.
  Mechanical, spec first. Reply "dedupe go".
- **TAX-1 no persistence-time code validation (HIGH):** invented codes (`PRIME_BLOOMBERG`,
  `INFORM`, `BUYER_OPTION_CONSULT`-under-pullRefile, …) are stored and rendered as real
  pills. A flag-only validation gate is safe; disposition of each existing invented code is
  a taxonomy call. Reply "codegate go" for the gate; the code list is in the review.
- **TAX-2 SOLICITATION_ACT scoping (HIGH):** the doc's "COR codes never on the no-shop list"
  rule is unimplemented AND may be wrong (leaked spans look like genuine text). This is a
  taxonomy-semantics decision — do you want the scoping enforced, or the rule relaxed?
- **TAX-4 four dictionary-less list-tagged keys** invent codes freely: author codebooks or
  de-tag `absenceOfChangesExceptions` / `undisclosedLiabilitiesExceptions` /
  `negativePreambleExceptions` / `instrumentVesting`.
- **DATA-1 GAP-A:** `reprocess.js --apply` never materializes claims — wire rematerialize in,
  or hard-warn. **DATA-2:** correction re-matcher (0.3 Jaccard, no margin) can misgraft a
  human correction; 73% of corrections rely on it. Both mechanical hardening, spec first.
- **PERF-1:** review API ships 7.5–10.4 MB/deal (resolvedReferences embeds full definition
  rows). Mechanical (reference pointers) — high demo value. Reply "perf go".

## 15. Taxonomy legal mis-codings (from the semantic audit — all your call)
Full evidence: `reports/` semantic audit. Families are 90%+ correct; these are the misses:
- **Endeavor** superiorProposalDeterminer BOARD → should be COMMITTEE (conflicted take-private).
- **Bring-down/materiality (weakest, 33/40):** HireRight + Carrols coded strictest when the
  "prevent-or-materially-delay" qualifier makes them loosest (add that code);
  **MAT_MAE_QUALIFIED vs MAT_MAE_AGGREGATE is applied at random** for identical drafting —
  merge the codes or re-normalize (cross-deal comparison on it is currently meaningless).
- **Governing law:** 10 deals carry both DELAWARE and SPLIT_FINANCING (nondeterministic
  single-value reads); SPLIT_FINANCING hardwires NY and mis-codes M.D.C.'s Japan carve-out
  as FOREIGN (`taxonomy.js:1040`).
- **Dictionary hygiene:** duplicate labels; "3-month LIBOR" tenor hardwired; no SOFR;
  no OTHER code anywhere while `extract.js:1723` mandates "pick the closest code" (force-fit).
