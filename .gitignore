node_modules
node_modules/
.vercel
.env*.local
.next/
data/
supabase/.temp/
.DS_Store
**/.DS_Store
* 2.md
* 2.json
.phase-id [0-9]*

# scratch query scripts (never commit)
scripts/_tmp_*

# stray agent scratch (never commit)
scratchpad/
reports/backups/
# machine run-artifacts (dry-run/apply reports) — keep .md summaries + decisions, drop timestamped json
reports/prune-cards-*.json
reports/mint-cards-*.json
reports/rematerialize-claims-*.json
.ingest-checkpoints/
