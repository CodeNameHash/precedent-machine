# Archived strings reviewer

This directory preserves the Phase 0-C strings-reviewer work from PR #140 as runnable reference code for Phase 0-D.

It is not wired into the app. The live admin routes were removed from PR #140 so that PR can stay inside the Phase 0-C allowlist.
